# s4 — "Source watch + change flag" · Acceptance scenario

Status: DRAFT — awaiting boundary approval.
No new screen → no mock (the surface is a CLI + CI artifacts in `visa-rules`).
Slice exits: `tdd` (watch logic test-first) + `code-review` — both mandatory.

## Why this slice is the risky one

The "live" promise (change → dataset within 48h, the primary success metric)
rests on it, and research flagged real hazards: official sites bot-wall plain
fetchers (make-it-in-germany: Radware; france-visas: 403), gesetze-im-internet
refuses agents but serves humans, and the ES values live in a
non-machine-readable PDF. Deferred three boundary sessions; entered now.

## Part 0 — DE source spike (decision before code)

Candidates for a stable DE value source, tried in order, outcome recorded in
DECISIONS.md and encoded in the watchlist:
(a) plain fetch of the ZAV newsletter *index* (edition URLs change; the index
    may be stable and fetchable), (b) plain fetch of BAMF value-adjacent
    pages, (c) headless fetch of make-it-in-germany, (d) human-read tier with
    a scheduled reminder (the ES model). Success = a watchlist entry whose
    strategy was demonstrated live once.

## Seed data

`watch/watchlist.json` in `visa-rules`: one entry per monitored source —
URL, fetch strategy (`html-selector` / `pdf-hash` / `human-tier`), extraction
selector where applicable, and the dataset value ids it backs.
`watch/state.json`: append-only snapshot store (normalized-content hashes +
retrieval timestamps).

## Steps

1. **Coverage is enforced, not promised**: a CI check fails if any
   `source_url` in the dataset is missing from the watchlist, or if the
   watchlist carries an orphan no dataset value references.
2. **`npm run watch`** fetches every non-human-tier source, extracts the
   monitored region, normalizes (whitespace, encoding), hashes, compares to
   state; ndjson log per source; exit code reflects worst outcome. State is
   only written with `--commit`.
3. **Change detected** (fixture: mutate a stored snapshot): the report names
   the source, both hashes, and a quoted context diff; a flag file is written
   to `watch/flags/` (becomes a GitHub issue when CI runs on GitHub — repo is
   not public yet, so real-green here = flag file + one live run against the
   real pages).
4. **Edge lines (mandatory under steward-3):**
   - a source answers 403/bot-wall → status `unreachable`, clearly distinct
     from `changed` — a blocked fetch must never read as "no change";
   - ES PDF byte-identical → quiet; byte-different → `changed (human must
     read)` flag, never an extracted value;
   - network fully down → loud failure with per-source statuses, never a
     silently green run;
   - a `human-tier` source (DE if spike lands there, gesetze re-verification)
     → scheduled reminder flag with `last_verified` age, no fetch attempted.
5. **Cadence**: GitHub Actions cron config (daily) committed and valid;
   actual scheduled firing lands with the public repo in s6 (stated honestly
   as a deferred bit, tracked on the kanban).
6. **Regression**: engine/site untouched; all 51 existing tests stay green.

## Invariants (property tests over generated content)

- Normalization is idempotent: `normalize(normalize(x)) === normalize(x)`.
- Whitespace/encoding-only variations of the same content never flag a
  change; any single-character change in the monitored region always does.
- A watch run without `--commit` leaves `state.json` byte-identical.
- Every dataset `source_url` ⊆ watchlist URLs (checked mechanically, step 1).

## Runs

- Part 0 spike: resolved 2026-09-02 without headless — ZAV edition index is
  stable and plain-fetchable (sentinel) + direct watches on the two value
  pages (decision in DECISIONS.md).
- mock-green: 2026-09-02 — 14 watch tests (outcomes, PDF, human tier,
  no-mutation, both-way coverage) + normalization property tests; 65 total.
- real-green: 2026-09-02 — LIVE run against the real pages: 5/5 automated
  sources fetched plain (no bot-wall on the spike-chosen set), baselines
  committed; hashes stable across two live fetches minutes apart (normalize
  works on real-world churn); change-detection demoed live via mutated state
  (flag file with quoted context produced); human-tier entry correctly "ok"
  without fetching. Deferred bit, tracked: the daily cron fires only once the
  repo is on GitHub (s6). Extra edge found & fixed during the run: BOM'd
  state/watchlist (Windows hand-edit) crashed the CLI → BOM tolerance added.
  code-review exit: 10 verified findings (9 CONFIRMED, 1 PLAUSIBLE) — the
  dominant theme was three independent alert-loss paths in the CI workflow
  (swallowed issue-creation against a never-created label while the baseline
  still advanced; exit-1 on one unreachable source discarding all detected
  changes; committed flags re-filing duplicate issues daily), plus verified
  hash blindness in entity normalization (&ge;/&le; collapsed to the same
  hash) and a crashing code-point path. All 10 fixed in
  `visa-rules@374c9c5`: persist-before-issues + continue-on-error with a
  final re-raise, new-flags-only issue loop, label ensured, unknown entities
  kept literal, exhaustive criterion walk (assertNever) feeding the coverage
  gate, stable reminder filenames, future-date clamp, derived negative
  coverage test. 69 tests green. Real-green stands on the fixed revision.

## De-mock births

- (none; the state-mutation in step 3 is a test technique, and the demo flag
  file was deleted — permanent fixtures exempt by rule)
