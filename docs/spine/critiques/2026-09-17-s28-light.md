# Light critique — s28 (learn links), local build

Light mode, not isolated; opens no gate. Scope from the diff: the three
surfaces that render a learn door — the question's help line, the result
card's box, the route page's "You can find this out yourself" — walked at
390×844 on the branch's local build; screenshots `s28-390-question.png`,
`s28-390-card.png` (scratch), opened as images. Controls operated: the
door on each surface (buzer.de opens in a new tab), *I don't know* on the
shortage question, the card's *Read the rules of this route*.

## Lenses (scope: the learn doors)

- **Copy & framing 5/5** — each door says what the reader does there in plain
  words; no statute shorthand, no explainer inside a link.
- **Trust surfacing 4/5** — the door names the law in words and keeps the
  ISCO codes; the provenance lines beneath still cite *§ 18g AufenthG* as
  the authority's own reference, which is right.
- **Design fidelity 5/5** — the box, the help line and the route block draw
  as before; the link is one underlined sentence in the band colour.

## Findings

None new. Seen, on the board already or outside the slice: the Algerian
notice's own `learn` label is a statement (*The page for Algerian nationals
— …*) rendered as its foot link — the same shape this slice fixed for the
four fields; a candidate for the next copy pass, filed under v1.2 fixes.
