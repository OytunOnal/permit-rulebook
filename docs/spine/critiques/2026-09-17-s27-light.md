# Light critique — s27 (five small slips), local build at 66c0820

Light mode, not isolated; opens no gate. Scope from the diff: the phone
menu, the four-country section summaries, the 404, the desktop ledger, a
route page header — walked at 390×844 and 1280×900 on the branch's local
build; screenshots `s27-390-menu.png`, `s27-390-404.png`, `s27-390-secs.png`,
`sections-1280.png`, `s27-1280-ledger.png`, `s27-1280-route.png` (scratch),
each opened as an image.

Controls operated: Menu (open → *Close*; Escape → *Menu*); a country
summary (closed/open); the 404's *Check your own situation*; ✎ on a ledger
row; the route page's *Check yours* button.

## Lenses (scope: these five screens)

- **Copy & framing 4/5** — *Close* says what the button does; the 404 says
  its sentence once; the route H1 is the name and the tagline its own line.
- **Design fidelity 5/5** — the mark sits on the name, the tally right; the
  pen on the answer's last line; the tagline in the hero's face and the
  accent colour, one size down; nothing off-palette.
- **Accessibility 4/5** — the 404's labelling heading is visible and read
  once; `aria-expanded` and the text agree. (The Menu button widens by one
  letter when open — seen, cosmetic, not in the scenario; noted for P-list.)
- **Orientation 5/5** — unchanged.

## Findings

None new. One cosmetic observation for the board: the phone Menu button's
width changes with its word (a `min-width` sized to the wider word holds
it).
