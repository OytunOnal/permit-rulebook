# Light critique — s26 (one reader, one hierarchy), preview at 95f0bd0

Light mode, not isolated (the session that did the work); opens no gate.
Scope from the diff: the four-country result when nothing is open and the
reader named a country at question 3 — walked at 390×844 and 1280×900 on the
branch build (local preview), the research/France reader with nothing open,
then the same reader with a German route opening; screenshots
`s26-390-zero.png`, `s26-1280-zero.png`, `s26-390-open.png`,
`s26-1280-open.png` (scratch), each opened as an image.

Controls operated: *read its rules* (→ `/france/talent-researcher/`), the
France summary (open by default; closes and reopens), Germany's summary
(collapsed; opens), the route link in France's line, ✎ on *Offer country*.

## Lenses (scope: this result only)

- **Copy & framing 4/5** — the headline is the direct path's sentence, the
  subline names the route and the door, and *France's routes below need a
  job offer or an intra-corporate transfer* is true of what follows. Held
  from 5 by the sentence appearing twice on one screen (masthead and
  France's line) — the scenario's decision (point 3), and the second is the
  row a skimming reader lands on.
- **Result actionability 4/5** — one link to the quoted route in the first
  two lines, France first and open; the German unlocks appear when they
  exist (the seeded-screens case). Held by what F1/F4 still hold.
- **Design fidelity 4/5** — palette, type and the stamp as designed; the
  strip in its place. The France summary line's triangle-left / name /
  sentence-right layout is P8's finding, on the board, unchanged here.
- **Orientation 5/5** — header, countries, the door, *Start over*; the
  sidebar's ✎ returns to question 3 and back to the verdict (s20).

## Findings

None new. Delta against the v1.1 re-score's polish item: the "one reader,
two hierarchies" residue is gone on the walked path.
