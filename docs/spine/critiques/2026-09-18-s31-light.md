# Light critique — s31 (the hold state), local build at 05c62af

Light mode, not isolated; opens no gate. Scope from the diff: the returning
reader's first paint (module held 4 s), the verdict after a correction
(focus), the desktop page with classic scrollbars — at 390×844 and
1280×1400 on the branch build; screenshot `hold-s31-390.png` opened as an
image; the focus read from `document.activeElement`.

Controls operated: reload with a finished record (the hold, then the
verdict); ✎ from the verdict, an answer, the return; Tab from the headline
after the return; a scroll on the desktop verdict.

## Lenses (scope: these three states)

- **First-run clarity 5/5** (the returning reader) — header, eyebrow, one
  sentence that says what is happening; the verdict lands where the
  sentence was.
- **Accessibility 5/5** — after a correction the focus and the announcement
  agree (the headline); Tab goes on into the result; first arrivals leave
  the focus alone.
- **Design fidelity 5/5** — the line in the subline's dress; no jump when
  the desktop page grows a scrollbar.

## Findings

None new. The two Back behaviours and the unknown link's first paint stay
on the board as filed.
