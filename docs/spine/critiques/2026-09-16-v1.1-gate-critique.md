# Product critique — Permit Rulebook v1.1 gate (2026-09-16) · PRODUCT_RUBRIC 1.3 · **isolated run**

Run by `spine:product-critique-agent` against the **deployed** product at
`https://permitrulebook.com` (deploy of 2026-09-16, commit `de094a7`). The agent
built none of v1.1 and applied none of the previous run's adjustments.

**Blindness.** `DECISIONS.md`, `KANBAN.md`, `STATUS.md`, the git log and every
file under `docs/spine/critiques/` and `docs/spine/critique-*.md` stayed closed
until all ten scores were written. Open during the walk: `docs/spine/one-pager.md`
(personas) and `tokens.css` (screenshot pass). `docs/spine/design/site-map.md`
was opened after the scores were written, for the Orientation lens only, and
changed nothing. The previous report (`2026-09-08-v1-gate-critique-2.md`,
RUBRIC 1.3, 36/50) was opened last, for the delta.

**Rubric.** PRODUCT_RUBRIC **1.3**, the same version as the previous run — the
scores **are** comparable and are subtracted lens by lens below.

**Browser.** Two drivers. The repo's own headless harness (`scripts/browser.mjs`,
CDP, `Emulation.setDeviceMetricsOverride`) at **390×844** (mobile on) and
**1240×900** for the walks and ~110 screenshots, each interview step and every
result scroll-window captured; the connected Chrome (1920 wide, real mouse and
keyboard) for the header disclosure's real-event tests (open · Escape · click
outside). Every screenshot cited was opened **as an image** with `tokens.css`
beside it. The site threw **zero** console errors or exceptions across every
walk (`page.problems()` empty on each run).

**One condition to declare.** The Chrome window could not be resized (it reports
`innerWidth 1920` whatever is asked), so the *real-tap* phone test of the
result's correction rows was done in the headless harness with synthetic
clicks. The scroll defect it found (B2) is layout-driven — the document shrinks
and the viewport is left where it was — so it does not depend on the event
source; it is nonetheless listed under "for the human only" as the one thing
worth a thumb on a real phone.

---

## Personas walked

The one-pager names one user — a non-EU knowledge worker looking at DE/FR/ES/NL
who has or is about to have an offer — with the Türkiye-agreement case as the
named additive exception. Four walks were derived, plus the hostile walk.

| | Persona | Profile carried into the walk |
|---|---|---|
| P1 | **Deniz**, 32, Turkish software engineer, Berlin offer at €58,000 | Degree, has *not* checked Anabin ("I don't know"), IT so on the shortage list, 4 years' experience, A1 German, C1 English, €1,091+ funds. Phone first, then desktop. |
| P2 | **Priya**, 27, Indian data analyst, no offer yet, "any of these four" | Degree, recognition unknown, shortage unknown, 2 years' experience, no German, C1 English, no Dutch degree, IND designation unknown, €1,091+ funds. |
| P3 | **Rafael**, 35, Brazilian post-doc with a **hosting agreement in France** | The research-institution situation; walked again against **Spain** (P3′) to see the same answer land somewhere. |
| H | **Hostile** | Back-tracking (Back twice, change situation and come back), gibberish in the passport search, an EU passport (Italy), every "I don't know" the product offers, the experience ladder answered at each rung, a returning visit with a half-finished record. |

---

## Screens walked, and the controls **operated** on each

Every control listed was clicked (or typed into and activated) and its effect
observed. Controls named *not operated* are declared, not credited.

**`/` — interview, 390 and 1240.** Operated: destination `de`, `fr`, `es`, `nl`,
`all`; situation `offer`, `ict`, `research`, `none`; qualification `degree`,
`vocational`; the passport combobox by typing `Turk`, `Ind`, `Braz`, `Nig`,
`Ital`, `zzzq` (empty state) and picking `Türkiye`, `India`, `Brazil`,
`Nigeria`, `Italy`; the shortage question's `§ 18g AufenthG … ISCO-08` link and
the recognition question's `Anabin` link (hrefs read, both external); shortage
`yes`/`unknown`; recognition `unknown`; experience `lt2`, `y2in5`, `y3in7`,
`y5in7` (each run to a result and compared); yearly salary `band_6`, `band_3`,
`unknown`; German `none`, `a1`; English `c1`; age `u30`, `a30to35`, `o40`; funds
`band_1`, `unknown`; the Dutch-degree and IND-designation questions; stay and
spouse questions; `← Back` (twice, and the "Your answer — pick another to change
it, the rest are kept" screen it produces, with `Keep this answer`); the
pre-scoped entries `/?country=de`, `/?country=fr` (with a German record already
saved), `/?route=de-experienced-worker`, `/?route=fr-eu-blue-card`,
`/?route=de-researcher`; a return visit to `/` with a half-finished record and
with a finished one; keyboard focus on the first option (ring measured) and
activation by Enter. **Not operated:** qualification `none`; recognition
`recognized`/`partial`/`not_yet`; yearly bands 0, 1, 2, 4, 5, 7; German
`a2`/`b1`/`b2plus`; English `none`/`b2`; age `a35to40`.

**`/` — results (P1 Germany, P2 all four, P3 France, P3′ Spain, H unknown-heavy,
H Italy), 390 and 1240.** Operated: `The rules of this route` on the two open
cards and on a not-yet card (each lands on its route page); `Official page ↗`
hrefs read on every card (bamf.de, handbookgermany.de, europa.eu — all
`target=_blank`, external, not credited beyond the href); the `Check your degree
in the official Anabin database ↗` box (href read); the collapsed `NOT YET`
`<details>` rows and the collapsed **country** `<details>` in the four-country
result (France and Netherlands opened by hand); the `You declared` `<details>`
(opened; its twelve `role=button` rows — `Situation`, `Yearly salary`, `German`
each tapped, the question re-shown with the current answer ticked, re-answered
with the same value and with a different one, the verdict recomputed:
"2 routes look open." → "1 route looks open. One is short by €4,274/year.");
`Keep this answer`; `Start over` (localStorage verified empty afterwards, question
1 shown); the line at the end, `Something to say about this result? Feedback.`
(href `/feedback/`).

**`/germany/experienced-worker/` — route page, 390 and 1240.** Operated:
`Check yours — Germany, Experienced worker (§ 19c / § 6 BeschV)` (lands on
question 2 with "Coming from Experienced worker … — Germany is on the record.");
the data door `This route as JSON` (`200 application/json`, 6,336 bytes, the
route's record with source/quote/date); `The dataset on GitHub` and `Report what
is wrong` (hrefs read); the eight `Also in Germany` links (hrefs read, one
followed); every `Official page ↗` href read. Also read:
`/france/employee-card/` (a quoted-not-scored page) and
`/netherlands/orientation-year/` (200).

**`/france/` — country page, 390 and 1240.** Operated: the six route cards
(hrefs read, one followed); `Check yours — France` (`/?country=fr`, operated
fresh and with a saved German record); the `listed with the reasons` link
(GitHub `exclusions.md`, 200).

**`/data/` — 390 and 1240, top to bottom.** Operated: `tell us` (`/feedback/`);
`The whole dataset as JSON` (`200`, 235 KB); `countries.json` (`200`, 22 KB);
`The dataset on GitHub`, `Report what is wrong`; all 28 per-route JSON links
(hrefs read; six fetched, all `200 application/json`).

**`/feedback/` — 390 (headless) and 1920 (real Chrome).** Operated: the header
`Countries` disclosure with real events — click (opens over the page), **Escape**
(closes, focus returns to the summary), reopen, **click outside** (closes); the
phone `Menu` button (opens: `aria-expanded=true`, nav shown, first link focused;
second click closes; Escape closes; outside click closes); the three door
buttons — **hrefs read only**: each is a Gmail compose URL
(`mail.google.com/mail/?view=cm…to=feedback@permitrulebook.com&su=…`) opening in
a new tab, and each `or with your own mail app` is a `mailto:` — both leave the
page and are **not credited** as operated; the **copy glyph** with
`navigator.clipboard.writeText` stubbed — it received
`feedback@permitrulebook.com` and an `aria-live="polite"` "Copied" span appeared;
the GitHub links `Report a wrong value` and `Suggest a route or a change` (hrefs
read; both `github.com/OytunOnal/permit-rulebook-data/issues/new…`).

**404 (`/this-page-does-not-exist/`), 390 and 1240.** Operated: the four
country links and `Check your own situation` (hrefs read, `/`).

**Footer**, read on `/`, `/data/`, `/feedback/`, `/france/` and the route page:
identical markup on all — Countries (four links + `Check yours`), The data
(`/data/`, repository, licence; `This route as JSON` added on route pages),
Feedback (`/feedback/`, sponsors), the seal, and the line "values read between
2026-09-02 and 2026-09-10 · re-read daily (last run 2026-09-16 · 1 source
unread) · dataset 2026-09-10". `The repository`, `Licence · CC BY 4.0`,
`Sponsor this work` all resolve `200`.

---

## Persona walks

**P1 — Deniz (Turkish, Berlin offer, phone).** Q1 → Germany; the counter reads
"QUESTION 1 OF UP TO 24" under "A few quick questions.", then 15, 15, 14 … 12.
Twelve taps. The passport search found Türkiye on `Turk`, marked "a note applies
to this passport". The result: **"2 routes look open."** — Experienced worker
(§ 19c / § 6 BeschV) and the Opportunity Card, the three Blue-Card/§ 18b routes
in "Not yet" as "an open gap, not a no" with the Anabin link. The Türkiye notice
at the top is sourced and honest. What she reads next, under the unlock steps:
*"Your job-search permit above is exactly for this — lawful time in the country
to land that offer."* She has the offer. On the phone the "You declared" panel
is at the bottom of ~7,000 px of result; tapping `Yearly salary ✎` left her
looking at the footer (B2).

**P2 — Priya (Indian, exploring, any country).** Fifteen questions. The result:
**"Nothing open yet — 10 steps would change that."** and "Nearest: an
intra-corporate transfer in Germany." — she has no employer to transfer her.
Germany's section is open; France and the Netherlands are collapsed, the
Netherlands' "1 has an open unknown" (the orientation year she might reach)
hidden until tapped. The Opportunity Card card lists her two unknowns and the
Anabin/ISCO links but never prints her points — she is at 5 of 6 and the
screen does not say so; the unlock list says it six different ways instead.

**P3 — Rafael (Brazilian researcher, hosting agreement in France).** Two taps:
France, then "I have (or expect) a hosting agreement with a research institution
there". The interview ends. **"Nothing open on these answers."** — 0 open, 0
within reach, "5 routes need a job offer or an intra-corporate transfer". The
French researcher permit is not mentioned anywhere on the screen, on `/france/`,
or on the site; the only trace is a GitHub link "listed with the reasons". The
same two answers in **Spain** (P3′) gave "1 route looks open." in three taps,
CRITERIA MET on Researcher (Ley 14/2013 art. 72) — followed by "Also required —
not checked here: A hosting agreement", the thing he had just declared.

**H — Hostile.** Back twice and a changed situation: the "Your answer — pick
another to change it, the rest are kept" screen appeared and the counter
re-derived (15 → 13 → 15). `zzzq`: "No country matches. Try fewer letters, or
the country's own name." Italy: **"No work permit needed."** with the europa.eu
quote. Every "I don't know": "Nothing open yet — 3 steps would change that", 8
not yet, 5 with an open unknown, each unknown named on its card — designed, not
leftover. The experience ladder run at each rung: `y2in5` and `y3in7` produce
the **identical** result; `lt2` closes the Experienced worker route. A return to
`/` with a half-finished record lands on "QUESTION 3 OF UP TO 8" with no line
saying why.

---

## Findings

### Blockers

**B1 — A France reader with a research hosting agreement is offered the option,
then shown a rejection screen after two questions, with no word that the
French researcher permit exists.** *P3, `p3r-01-y0.png` (390) and the text
dump.* Q2 offers "I have (or expect) a hosting agreement with a research
institution there" for France; the interview ends on it and renders:

> "**Nothing open on these answers.** Your 2 answers were compared against 5
> published rule sets. … Change an answer to explore — every route below says
> what it asks for, and quotes it." · 0 OPEN · 0 WITHIN REACH · 5 NOT YET · "5
> routes need a job offer or an intra-corporate transfer"

`/france/` says "5 routes scored, 1 quoted" and lists no researcher route; the
exclusion list lives on GitHub. The passeport-talent *chercheur* is a route this
person can actually hold — the one-pager's own v1.1 criterion for a quoted page.
**User cost:** the affected reader is told, on a site whose claim is "every route,
quoted and dated", that nothing is open, when the truth is "your route is not
covered here". He leaves believing the first. This is the record's known failure
shape (a rejection screen for a person the rules do not reject) on a new
persona. *An answer that ends the interview is the strongest form of "an answer
that changes nothing": Q2's research option for France leads nowhere and should
say so before it is picked.*

**B2 — On a phone, tapping a "You declared" row to change an answer leaves the
question off-screen; the reader sees the footer or the unchanged panel, and
after re-answering is returned to the bottom of the new result, not its
headline.** *P1, `p1c-01-edit-viewport.png`, `edm-01-edit-landing.png`,
measured at 390.* After `Yearly salary ✎`: `scrollY 1811`, question card top at
**−1,366 px**, viewport showing "Permit Rulebook makes no immigration decision…"
and the footer columns. After `German ✎`: `scrollY 924`, card top **−479 px**,
viewport showing the "You declared" list, identical to before the tap. Focus
stays on `<body>` in both. After picking a new band the page rendered the new
verdict ("1 route looks open. One is short by €4,274/year.") and landed at
`scrollY 8088` — the panel again; the headline is ~8,000 px up. Desktop does
not have this: the panel is a sidebar and the card top lands at −15 px.
**User cost:** the correction control — the one thing the panel exists for —
looks dead on a phone. A reader taps, nothing visibly happens, and either taps
again or gives up; the one who scrolls up and finds the question, answers it,
and is dropped at the bottom never sees what changed.

### Friction

**F1 — The result addresses an offer-holder as a job-seeker, and ranks steps by
rule count rather than by what the reader can do.** *P1, `p1r-05-y3376.png`;
P2, `p2r-01-y0.png`; H.* Under "Steps that would unlock more": "Your job-search
permit above is exactly for this — lawful time in the country to land that
offer." — to a reader who declared "I have (or am about to get) a job offer
there". The three step groups are ordered *intra-corporate transfer · research
hosting agreement · full German recognition* for a person whose only real step
is the third; the four-country hero says "Nearest: an intra-corporate transfer
in Germany" to someone with no employer, and the unknown-heavy hero "Nearest: an
intra-corporate transfer" to an offer-holder. **Cost:** the differentiating
section opens with two steps the reader cannot take and a sentence that
contradicts their declaration.

**F2 — The experience ladder is not a ladder, and the engine treats it as one.**
*Q7, `p1-08-after-unknown.png`; results for `y2in5` vs `y3in7` compared.* The
options are `Under 2 years` · `2+ years within the last 5` · `3+ years within
the last 7` · `5+ years within the last 7`. Two are true at once for most
readers; "3+ within 7" does not imply "2+ within 5", yet `y3in7` scores
Experienced worker (§ 6 BeschV, "two years of recent skilled work in the last
five") as CRITERIA MET exactly as `y2in5` does. **Cost:** a reader with three
years of work six-to-seven years ago is told a route is met that its own quoted
rule closes. *(Third run for the overlapping bands — previous F19.)*

**F3 — Every route page warns "the last run did not reach one of them" as if one
of its own sources were unread; `/data/` says the unread source is German.**
*`route-02-y844.png`; `/france/employee-card/` text.* The sentence "a daily
check re-reads every source; the last run did not reach one of them" appears
verbatim on the German route page **and** on the French employee-card page. On
`/data/`: "A German source did not answer on the last run; the values it backs
were read on 2026-09-07." **Cost:** on 27 of 28 route pages the liveness line
casts doubt on sources that were read fine, and never names the one that was
not.

**F4 — The not-yet Opportunity Card hides the points tally, the one number that
decides it.** *P2, text dump; compare P1's open card "6 points — 6 needed ·
English +1 · Experience +2 · Shortage list +1 · Age +2".* Priya's card prints
her two unknowns, the Anabin and ISCO links and six quotes, but not "5 points —
6 needed". The unlock list then says "would be met" six times (full
recognition, partial recognition, 5+ years, German A2, German B1, German B2 or
higher) — three of them rungs of one ladder. **Cost:** the reader nearest to a
verdict cannot see how near. *(The salary-ladder enumeration of previous B3 is
gone; this is its smaller cousin on the CEFR enum.)*

**F5 — A returning visitor lands mid-interview with no notice.** *`resume`
walk; `p-resume` text: "QUESTION 3 OF UP TO 8 / Your highest completed
qualification?".* Answers are restored from the device and the reader is put
on the next unanswered question; the only clue is the collapsed "You declared —
2 answers" box below the card on a phone. The pre-scoped entries do this well
("France is on the record now. Your answers about … are kept; France asks 2
more questions") — the plain `/` entry says nothing. **Cost:** a second visit
reads as a broken first question. *(Previous F18, still open.)*

**F6 — After each answer, keyboard focus drops to `<body>`.** *ctrl walk:
"after Enter/click on opt: focused: BODY".* The focus ring is present and
visible (2 px `--color-stamp`), the counter is a polite live region — but the
next question's options are not focused, so a keyboard reader Tabs through the
header (wordmark, Menu / Countries, Check yours, The data, Feedback) before
every question. **Cost:** five extra keystrokes per question on desktop, a
lost place on every one.

**F7 — In the four-country result, France and the Netherlands are collapsed to
one line, and the line hides what it counts.** *P2, `p2r` DOM: `DETAILS.country-sec
:: France 5 not yet` (closed), `Netherlands 6 not yet — 1 has an open unknown`
(closed).* The Netherlands' open unknown — the orientation-year designation she
answered "I don't know" to — is the most reachable thing on her screen, and it
is behind a closed summary with no name. **Cost:** the country most likely to
open for a fresh graduate under 30 is the one folded away.

**F8 — The Spanish researcher card says a hosting agreement is "not checked
here" to a reader who just declared one.** *P3′, text dump: "Also required —
not checked here: A hosting agreement — the formal agreement a research
institution signs to take you on — or an employment contract with the research
body".* Q2's research option *is* that declaration. **Cost:** the card's honesty
device ("not checked here") is spent on something that was asked, which teaches
the reader to discount it where it matters.

**F9 — Two hints ask for knowledge the reader does not have.** *Q5
(`p1-06-after-pick_T_rkiye.png`), Q8 (`p1-09-after-y3in7.png`).* "Not sure?
§ 18g AufenthG … lists the shortage groups by ISCO-08 code (132, 133, 134, 21,
221, 222, 225, 226, 23, 25)" — a layperson does not know their ISCO code; the
salary question is nine bands cut to the cent ("€45,630 – under €45,934.20",
a €304 band) with nothing saying why the cuts fall there. **Cost:** a hesitation
on two of twelve questions. *(Previous F8 and F12; the bands now say "under",
which cleared previous B1 — the cents remain.)*

### Polish

**P1 — The statute explainer stutters and varies between renders.** Headings
read "Experienced worker (§ 19c / § 6 BeschV; sections 19c and 6)", "ICT Card —
a transfer within your company (§ 19, section 19)", "Opportunity Card
(Chancenkarte, § 20a, section 20a)", "Researcher (§ 18d, section 18d)" — the
same route is "(§ 20a)" on one render and "(§ 20a, section 20a)" on the next
depending on which card comes first. *`p1r-02-y844.png`, `p2r-01-y0.png`.*

**P2 — Generated rule prose reads as machine output.** Route page:
"2+ years of related experience or 5+ years of related experience — CONDITION —
This route asks for 2+ years of related experience or 5+ years of related
experience."; "Either of these answers this rule:" before three items; the
results not-yet line "Needs 2+ years of related experience or 5+ years of
related experience". *`route-04-y2532.png`.*

**P3 — A curator's note is on the user's card.** "§ 20a Absatz 2 AufenthG
(consolidated mirror — gesetze-im-internet.de times out from here)" on the
Opportunity Card. *`p1r-04-y2532.png`.*

**P4 — Modelling vocabulary on user-facing lines.** "quoted and dated · scored,
two conditions stated but not asked" on every card; "one in our own reading" on
`/france/` cards; "0 standing on a dated reason" on `/data/`; "scored against
your answers". *(Previous F5, reworded, still internal.)*

**P5 — A stray full stop.** The Anabin box on a phone renders "Check your degree
in the official Anabin database ↗" and then "." alone on the next line.
*`p1r-06-y4220.png`.*

**P6 — "A few quick questions." above "QUESTION 1 OF UP TO 24".** *`p1-01-q1.png`.*
The denominator then reads 15 / 14 / 12 (P1), 24 / 15 (P2), 11 / 7 (NL), 7 / 3
(ES). *(Fourth run — previous F1.)*

**P7 — The Türkiye notice names a Dutch route to a Germany-only reader.**
"…that route's card says so — the Dutch orientation year is one." on a result
scoped to Germany. *`p1r-01-y0.png`.*

**P8 — Small rendering and labelling slips.** The phone `Menu` button still
reads "Menu" when open; the four-country result's country summaries put the
disclosure triangle at the far left and the country name at the far right
(`p2r-01-y0.png`); the 404 stacks an H2 "Start from your own situation" over a
bold "Or start from your own situation."; the `✎` glyph wraps to its own line
in the desktop sidebar ("University degree ⏎ ✎"); the route page's H1 ends
"…BeschV; sections 19c and 6)." with a full stop after the parenthesis.

**P9 — `/data/`'s route list still leads to JSON.** *(Previous F9.)* Twenty-eight
route names under "Every route is also served on its own" link to `.json`; a
reader scanning for the German list clicks a name and gets a file.

**P10 — On a phone every question sits under the full hero and stamp.** The
question card starts at ~445 px on each of twelve screens (`p1-10-after-band_6.png`);
the first option is visible at 844 tall, and would not be on a 667-tall phone.
*(Previous P10.)*

### What is working, and must not be traded away

- The Türkiye notice: sourced, dated, honest about what the agreement does and
  does not do, at the top of the result.
- "An open gap, not a no", each unknown named on its card with a link to find out.
- "The source writes 45.630 where this page writes 45,630 — the same number."
- The pre-scoped entries: "Coming from Experienced worker … — Germany is on the
  record." and "France is on the record now. Your answers about … are kept;
  France asks 2 more questions." — the previous run's B2, cleared and narrated.
- "Nothing open yet — 3 steps would change that." in the unknown-heavy state,
  where the previous run counted thirteen: the salary-ladder enumeration is gone.
- The EU-passport screen, the empty search state, the 404, the quoted-not-scored
  page ("Why this route is not scored here"), `/data/`'s four named gates.
- One header everywhere; Escape and outside-click both close the disclosure;
  the phone menu focuses its first link; the copy glyph announces "Copied".
- Zero console errors on every walk; every data link 200.

---

## Prose pass

All user-facing strings from the walks were read as a list, away from the DOM.
Source-language residue: the German, French and Spanish quotes are the product's
declared record and are labelled ("German, from arbeitsagentur.de"); the route
*names* carry their native terms in brackets with an English lead ("Sent by your
employer abroad (salarié détaché ICT)") — fine. What surfaced only here:
modelling vocabulary (P4: *scored*, *stated but not asked*, *in our own reading*,
*standing on a dated reason*); the curator note (P3); the generated tautologies
(P2); the statute stutter (P1); one unexplained abbreviation set (ISCO-08 codes,
F9); and the sentence written for a job-seeker read by an offer-holder (F1).

## Screenshot pass

Every screenshot was opened as an image against `tokens.css`. Palette, type
scale, the stamp geometry, the rail, the double rules and the card top band all
match variant D at both widths; no off-palette colour, no style collision, no
horizontal overflow at 390. Defects seen only in the pictures: the stray "." (P5),
the far-apart triangle and country name (P8), the wrapped `✎` (P8), the three
ticks on the route page's rail overlapping at €45,630/€45,934.20 with no legend
tying tick to label (`route-04-y2532.png`).

---

## Rubric scores — PRODUCT_RUBRIC 1.3

Locked before the previous report was opened.

| Lens | Score | Evidence (one sentence) |
|---|---|---|
| 1 First-run clarity | **4** | What it is, how it decides, where the answers go and what the end looks like are above the first question at both widths (`p1-01-q1.png`, `q1d-01-view.png`); "A few quick questions." over "UP TO 24" is still the one contradiction (P6). |
| 2 Flow friction | **3** | One tap per question with a re-deriving counter, a Back that keeps the rest, and correction rows that recompute the verdict — against a Q2 option that ends the interview on a rejection (B1), a correction path that strands a phone reader (B2), a non-exclusive experience ladder (F2) and a silent resume (F5). |
| 3 Copy & framing | **3** | The verdict vocabulary is careful ("an open gap, not a no", "Also required — not checked here") but the cards still print modelling tiers, the statute explainer stutters, a curator note and a tautology reach the reader, and one sentence tells an offer-holder to go find an offer (P1–P4, F1). |
| 4 Trust surfacing | **4** | Quote · source · statute · read date · language on every value, the numeral-format note, four named gates and a per-route date on `/data/`, and the pre-scoped entries now narrate what is kept — held off 5 by the route pages' misattributed "did not reach one of them" (F3) and by "not checked here" on a thing that was asked (F8). |
| 5 Result actionability | **3** | The unlock steps name the exact route each change opens with the euro gap, and every unknown carries its link — but the steps open with two the reader cannot take, the hero's "Nearest" is an ICT for people with no employer, and the card nearest to a verdict hides its points (F1, F4). |
| 6 Edge states | **3** | The EU passport, the empty search, the unknown-heavy state, the 404 and the not-scored page are each designed and written — and the France-researcher answer is not: a rejection screen for a person whose route is simply absent (B1), with the four-country result folding a live unknown away (F7). |
| 7 Accessibility basics | **4** | One 2 px `--color-stamp` focus ring, Escape closing both disclosures with focus returned, polite live regions on the counter and the copy glyph, results sections under H2s, one control under 44 px measured on the result — but focus falls to `<body>` after every answer (F6). |
| 8 Responsive | **3** | Every screen holds at 390 with no overflow, the menu and the disclosure work, and the interview clears the tap floor — but the result's correction control strands the phone reader at the footer and returns them to the bottom (B2), and each question starts 445 px down (P10). |
| 9 Design fidelity | **4** | The rendered product is variant D as `tokens.css` describes it at both widths; the deviations are small and local — the stray ".", the split country summary, the wrapped `✎`, the unlabelled rail ticks (P5, P8). |
| 10 Orientation | **4** | One header and one footer on every page, the current page and country marked, `/data/` and `/feedback/` one step from anywhere, the 404 designed, no orphan found, and the pre-scoped entries now say what they kept — short of 5 because on a phone a route page's H1 does not name its country and the marked country is behind `Menu`, and `/data/`'s route names still open JSON (P9). |

**Total 35/50.**

Every lens at 3 is backed by a blocker or friction finding: Flow friction by
B1/B2/F2/F5, Copy by F1/P1–P4, Result actionability by F1/F4, Edge states by
B1/F7, Responsive by B2. No lens scored 2 or below.

### Delta against the previous run (`2026-09-08-v1-gate-critique-2.md`, RUBRIC 1.3, 36/50)

Same rubric version — the scores are comparable and are subtracted.

| Lens | v1 (09-08) | v1.1 (09-16) | Δ | Why |
|---|---|---|---|---|
| 1 First-run clarity | 4 | 4 | 0 | Same screen, same one contradiction (their F1 → P6 here). |
| 2 Flow friction | 3 | 3 | 0 | Their B1 (band edges) is **cleared** — every band now says "under"; two new blockers take its place (B1, B2). |
| 3 Copy & framing | 3 | 3 | 0 | Their F2 (glued date/language) **cleared**; their F5 (coverage tiers) reworded but still internal (P4); new stutter and tautologies (P1, P2). |
| 4 Trust surfacing | 4 | 4 | 0 | Their F4 (no translation) is policy and stands; new F3 (misattributed unread-source line). |
| 5 Result actionability | 3 | 3 | 0 | Their B3 (salary-ladder enumeration) **cleared** — 13 steps became 3; the CEFR ladder still enumerates (F4) and the ordering/"Nearest" problem is new (F1). |
| 6 Edge states | 4 | 3 | **−1** | **A drop, and a finding: B1.** The previous run walked no research-situation reader; the France dead end is an undesigned state on a persona the one-pager names. |
| 7 Accessibility basics | 4 | 4 | 0 | Their F16 (no H2 / live region on results) **cleared**; their F6 (tap targets) cleared to one control; focus-to-body (F6) is the residue. |
| 8 Responsive | 4 | 3 | **−1** | **A drop, and a finding: B2.** The previous run operated `✎` at 1100 only; at 390 the correction path strands the reader. |
| 9 Design fidelity | 4 | 4 | 0 | Their F2/P4/P6 rendering slips cleared; new small ones (P5, P8). |
| 10 Orientation | 3 | 4 | **+1** | Their B2 (pre-scoped entry answers about the wrong country for a returning reader) **cleared** and narrated; their F15 cleared; F9 (JSON list) remains (P9). |
| **Total** | **36** | **35** | **−1** | Two lenses dropped, one rose; both drops carry a blocker. |

**Previous blockers, verified by operation:** **B1** (shared band edges) —
cleared, "€45,630 – under €45,934.20". **B2** (pre-scoped entry silent for
returning readers) — cleared, "France is on the record now. Your answers about
… are kept; France asks 2 more questions." **B3** (salary ladder printed one
rung at a time) — cleared, "Nothing open yet — 3 steps would change that."

**Carried over unfixed, named again:** their F1 → P6 (fourth run); F5 → P4
(reworded); F7 (CEFR letters, no plain-language door) — still so; F8 → F9
(ISCO); F9 → P9; F12 → F9 (cents in the bands; the union ladder is now
per-destination for ES/NL/FR — the German list is the long one); F14 (hero
"short by €4,274" over a card that says "up to") — still so; F17 (no keepable
record) — still so; F18 → F5; F19 → F2 (third run, now with the wrong verdict
it produces pinned); P10 → P10.

---

## Adoption verdicts

**P1 — Deniz (Turkish engineer, Berlin offer).** *Would finish, would come back
after Anabin, would recommend — on desktop.* Twelve taps to two open routes with
the threshold beside her band and the three "open gaps" each pointing at the
one thing to check. **The single thing most likely to stop her:** on her phone,
tapping a "You declared" row does nothing she can see (B2); and the sentence
telling her to go land the offer she has (F1) is the first thing that makes
her doubt the rest.

**P2 — Priya (Indian analyst, exploring).** *Would finish; would come back only
if she happens to open the Netherlands row.* "Nothing open yet — 10 steps" is
honest, and the steps are real. **What stops her:** the screen never says she
is one point short of the Opportunity Card (F4) and tells her the nearest step
is a transfer she has no employer for (F1).

**P3 — Rafael (Brazilian researcher, France).** *Would not finish in any useful
sense, would not come back, would not recommend.* Two taps, "Nothing open on
these answers." **What stops him:** B1 — he was offered his situation and then
told it opens nothing, when the truth is that his route is not on the site.

**P3′ — Rafael, Spain.** *Would finish in three taps, would recommend.* CRITERIA
MET, quoted, with the year-after-to-look-for-work clause he did not know.
**What nearly stops him:** "Also required — not checked here: A hosting
agreement" (F8), which reads as the product forgetting what he said.

**H — Hostile.** *Would finish, and would be caught.* Back-tracking, gibberish,
an EU passport, every unknown, a returning visit — each landed on a designed
screen; the only thing that slipped past was the experience ladder (F2), where
a true-but-wrong rung is scored as met.

---

## Recommended Adjustments (pick-and-apply)

**1 — Close the dead end before it is picked, and name the missing route when it
is.** For a destination with no scored route on a situation, Q2 should say so on
the option itself ("Not scored for France yet — the researcher permit is quoted
here ↗" or "no route here asks this"), and the zero-open result that follows a
single eliminating answer should render a written state — "France's researcher
permit (passeport talent – chercheur) is not covered here; the routes below are
the ones we score" — with the exclusion reason on the site, not on GitHub. The
same rule fixes F8 in reverse: a card must not list as "not checked here" a
condition the interview asked as Q2. *Clears **B1**, **F8**; turns the one-pager's
"~14 quoted pages" promise into a screen.*

**2 — Make the phone correction land on the question and return to the
verdict.** On a `✎` tap: scroll the question card into view and move focus to
its first option (that also clears F6 for every question, since the same
focus-move belongs after each answer). On re-answer: scroll to the headline and
the strip and announce the new verdict in the existing live region. And put a
`Change an answer` link in the strip at the top of the result so the panel is
not 7,000 px away. *Clears **B2**, **F6**; makes F5's silent resume fixable with
one sentence in the same place ("You were on question 3 — your 2 answers are
kept").*

**3 — One pass over the sentences the walk tripped on.** The statute explainer
(render "§ 19" once, never "(§ 19, section 19)"); the route-page condition
prose (a rule that is "2+ years" is not "2+ years or 5+ years"); the liveness
line on route pages (name the source or say it only on `/data/`); the
job-search sentence (show it only when the reader declared no offer); the
curator note on the Opportunity Card; the modelling tiers on cards ("Every rule
that decides this route is asked; two more the authority states are listed, not
asked"); and print the points tally on the not-yet Opportunity Card. *Clears
**F1**, **F3**, **F4**, **P1–P4**.*

Pick: **apply all**, **apply 1 and 2**, or name a subset. Adjustment 1 protects
the promise — a person is never shown "nothing open" for a route that is merely
absent. Adjustment 2 is the phone; half the readers the one-pager expects arrive
on one. Adjustment 3 is a morning's copy work with three findings behind it.

Remaining findings become tracker issues. `design-flaw`: F2 (the ladder — a
data/engine judgment, see below), F5, F7, F9, P5–P10. `new-need`: a keepable,
shareable record (previous F17, still open); the exclusion list as a page on the
site (previous P2, still open, and now load-bearing for B1).

---

## For the human only

Everything below is a device or a judgment this run could not reach. Nothing
here is a control the agent could have operated and skipped.

1. **B2 on a real phone.** Open `permitrulebook.com` on your phone, answer to a
   result, scroll to the bottom, open "You declared", tap `Yearly salary ✎`.
   **Pass:** the salary question's heading is on screen immediately after the
   tap. **Fail (what this run saw):** the footer, or the unchanged panel. Then
   pick a different band. **Pass:** the new headline ("… routes look open") is
   on screen. **Fail:** the panel at the bottom.
2. **The three door buttons on `/feedback/`.** Each is a Gmail compose URL in a
   new tab; the "or with your own mail app" link is a `mailto:`. This run read
   the hrefs and did not credit them. **Pass:** on your desktop, `Report what is
   wrong` opens Gmail with subject "Wrong information" and the four headings in
   the body; the `mailto:` opens your mail app.
3. **Scope judgment for B1.** Whether the French researcher permit (passeport
   talent – chercheur) is scored, quoted-not-scored, or excluded is yours to
   rule; what the screen must not do is present the situation as askable and
   then say nothing is open. If it stays excluded, Q2 needs to say so for France.
4. **F2 is an engine judgment.** Either the ladder becomes one axis ("2+ years
   in the last 5" yes/no, then "5+ in the last 7" if needed), or `y3in7` stops
   satisfying a two-in-five rule. Which is right depends on how § 6 BeschV and
   the Chancenkarte points table read the same fact; the walk can only show
   that today the two rungs are scored alike.

---

## Re-score after s19 and s20 (2026-09-16)

Same critic, same rubric (**PRODUCT_RUBRIC 1.3**), against the **deployed**
product at `https://permitrulebook.com` (deploy of 2026-09-16 evening, commit
`1ca9e2c`). Scope: only what B1 and B2 and their lenses touch — the France
research answer, the Spain researcher card, the destinations that must carry no
mark, the correction path from *You declared*, the returning-visitor line, the
focus after each answer, and the layout shift on a record arrival. Nothing else
was re-walked; every unchanged lens below says so and why. `DECISIONS.md`,
`KANBAN.md`, `STATUS.md` and the scenarios stayed closed.

**Browser.** The repo's headless harness (`scripts/browser.mjs`, CDP, device
metrics override) at **390×844**, **390×1400** and **1240×900** (1280×900 for
the shift measurement, the s10 harness's own width). Every control named below
was clicked or typed into and its effect read from the DOM and the screenshot.
The site threw **zero** console errors or exceptions on every walk. The shift
measurement needs the module held back 700 ms, which a live origin cannot do,
so it was run on the repo's `dist/` — verified byte-identical to the deployed
`index.html` (11,163 bytes) with the same module hash (`CaKxHhKm.js`, 232,160
bytes on both) — served by `serve(dist, { delayJsMs: 700 })`, seeded with
records **harvested from the live product's own `localStorage`**, not typed by
hand.

### B1 — cleared on the path it was pinned to; the same defect survives on the four-country path (new N1)

**Operated.** `/` → `France` → question 2 at 390 and 1240. The research option
now carries a mark directly under it:

> Not scored for France yet — Talent — researcher (chercheur) is quoted, not
> scored: **read it here** ↗.

Markup: `<small class="opt-mark" data-for="research">` as a sibling after the
button; the link is `/france/talent-researcher/` (internal, no `target`). The
option itself is still pickable; picking it renders — at both widths — a
written state, not the rejection:

> **No scored route in France takes a research hosting agreement.**
> France's route for a research hosting agreement — Talent — researcher
> (chercheur) — is quoted here but not scored: **read its rules**. The routes
> below need a job offer or an intra-corporate transfer.

"Nothing open on these answers." is gone from this screen; the H1 above is what
the polite live region announces. The subline's link is
`/france/talent-researcher/`; followed, it is a real page (`200`, title
"Talent — researcher (chercheur) · France · Permit Rulebook"): "WHY THIS ROUTE
IS NOT SCORED HERE", then the fiche's own sentences — *"Avoir une convention
d'accueil souscrite avec un organisme public ou privé agréé ayant mission de
recherche ou d'enseignement supérieur…"*, *"Avoir un diplôme au moins
équivalent au master"*, the €2,200 pay floor with "The fiche names no period
for the figure" — each "service-public.gouv.fr F16922 — Carte talent, onglet
Chercheur · read 2026-09-16 · French, from service-public.gouv.fr", an
"Official page ↗" per condition, an "Our reading, not the authority's words"
box on the two-conventions ambiguity, the JSON door, and "Also in France" with
the six other routes. `/france/` now reads "5 routes scored, 2 quoted" and
lists the researcher card under "QUOTED HERE, NOT SCORED". Below the new
headline the screen is otherwise the old one: the strip still reads
`0 OPEN · 0 WITHIN REACH · 5 NOT YET` and the five cards each say "Needs a job
offer — you declared I have (or expect) a hosting agreement…" — honest, but the
green "0 OPEN" box is still the first number the reader meets after being told
why.

`/` → `Spain` → the same answer → passport `Türkiye`: "1 route looks open.",
CRITERIA MET on Researcher (investigación, Ley 14/2013 art. 72), and the
condition now reads **"Asked in the interview — you declared 'I have (or
expect) a hosting agreement with a research institution there': A hosting
agreement — the formal agreement a research institution signs to take you on —
or an employment contract with the research body"**. "Also required — not
checked here" is gone from that card. **F8 cleared.** Spain's question 2
carries no mark (correct: the route is scored). `Germany`, `Netherlands`, `Any
of these four`: question 2 rendered with **no `.opt-mark`** on any option —
verified by DOM query on each.

**Verdict on the persona.** Rafael, France path: two taps, and the screen
tells him the truth — his route exists, is quoted, and here is its page. He is
no longer shown a rejection. **Cleared.**

**But the same Rafael who picks "Any of these four — show me everything" at
question 1 is still shown one — N1, below.**

### B2 — cleared

**Operated**, with a complete twelve-answer German record (P1's profile: offer,
degree, Türkiye, shortage yes, recognition unknown, 3+ in 7, band 6, A1, C1,
30–35, €1,091+), at 390×844, 390×1400 and 1240×900:

- Result: "2 routes look open." Opened *You declared* (`<details>`, closed on
  the phone at document y = 7,023; a sidebar, open, at 1240). Tapped
  `#decl [data-field="salary_eur_year"]` (a `role=button` div).
- **After the tap, 390×844:** `section.qcard` top at **0 px**, its heading
  "Gross yearly salary for the job you would do there?" at 58 px, the first
  option "under €33,085.09" at 120 px and **focused** (`document.activeElement`
  is `button.opt`, focus ring drawn), the current band ticked ("€50,700 –
  under €59,373 ✓"), `← Back` and `Keep this answer` under the list. Same
  numbers at 390×1400. At 1240×900: card top 0, first option at 90 px, focused.
- **Picked `band_3`:** the page re-rendered "**1 route looks open. One is short
  by €4,274/year.**" with the H1 at **49 px** from the top of the viewport
  (scrollY 81) at 390×844 and 390×1400, at 50 px (scrollY 103) at 1240; the
  polite live region carries the new headline. The reader lands on the verdict,
  not the panel. Focus after the re-answer is on `<body>` — the announcement
  covers it; noted as polish, not as the blocker.

The previous run's numbers (card top −1,366 px, footer in view, landing at
scrollY 8,088) are gone at every width. **Cleared.**

**Returning visitor (F5), operated.** Three answers (`de`, `offer`, `degree`),
then `/` reloaded: inside the question card, above the counter —

> Continuing where you left off — 3 answers kept. **Start over**

`<p class="resumed">` with `<a class="edit" id="restart">`; count correct on
both 390 and 1240. `Start over` clicked: `localStorage` empty afterwards,
"QUESTION 1 OF UP TO 24" shown, focus on the first option. A *finished* record
reloads straight to its verdict with no line — acceptable; it is a result, not
a mid-interview screen. **F5 cleared.**

**Focus after each answer (F6), operated.** After `Germany`: focus on
`button.opt` "I have (or am about to get) a job offer there" (top 550 at 390,
427 at 1240). After `offer`: `button.opt` "No completed qualification". After
`degree`: `input#cfilter` (the passport search). The keyboard path no longer
drops to `<body>` between questions. **F6 cleared.** (Small caveat for a real
phone: on a resumed arrival the passport *input* is focused programmatically;
whether a keyboard pops is the OS's call and was not observable here.)

### Layout shift on a record arrival — the builders' "+44 px" confirmed in kind, corrected in number; and one arrival the gate has never measured

Module held 700 ms, records harvested from the live product, the s10 test's own
timing (250 ms settle for the built paint, then the observer with
`buffered: true`):

| arrival (real record) | 390×844 | 390×1400 | 1280×900 |
|---|---|---|---|
| half record, 3 answers, next = passport | CLS 0.002 · footer **+72 px** | 0.037 · +72 px | 0.000 · −5 px |
| half record, 4 answers, next = shortage list | CLS 0.003 · footer **+121 px** | 0.035 · +121 px | 0.005 · +59 px |
| **finished record (12 answers)** | **CLS 0.654 · footer +6,199 px** | **0.764 · +6,199 px** | **0.564 · +4,259 px** |

The half-record arrivals are inside Google's "good" (< 0.1) at every width;
the builders' figure is right in kind. The footer travel is not a constant,
though: it is the resumed line **plus the difference between the next
question's card and question one's** — +72 px when the next question is the
passport search, +121 px when it is the shortage list. "+44 px" is one record's
number, not the arrival's.

The **finished** record is the finding. What the build paints — "Which
work-permit routes could fit? / A few quick questions." (90 px), the *Rules
read* stamp, the box with "Your answers are on this device — bringing them
back." — is replaced 700 ms later by "2 routes look open." (31 px), a 71 px
subline, the *Record generated* stamp and a 6,590 px result. The shift sources
are `#app, #subline, #stamp` — the masthead, the one part s10 promised would
never move on any arrival. The s10 harness reports this arrival as "0 · −2 px"
because its seed is not a finished record: it writes `education`, `age` and
`salary_eur_year: "60000"`, where the live interview stores `qualification`,
`age_band` and `band_6` (the harvested record:
`{"destination":"de","situation":"offer","qualification":"degree","citizenship":"TR",…,"salary_eur_year":"band_6",…,"age_band":"a30to35","funds_eur_month":"band_1"}`).
Replayed, the seed keeps four answers and lands on a question — so the row
labelled "finished record" measures a half record, and the test's own "resumed
line is there" assertion passes on it for that reason. Whether s19/s20
introduced this the screens cannot say; what they say is that the gate has
never seen it.

### New findings

**N1 — Blocker. On the four-country path the France researcher is still shown
"Nothing open on these answers." with no word of the researcher permit.**
*`b1-390-all-research-fr-no-result.png` and the text dump, 390.* `/` → `Any of
these four — show me everything` → `I have (or expect) a hosting agreement with
a research institution there` (no mark: correct, three of four destinations
score it) → question 3 "Which country is that offer, transfer or agreement in?"
→ `France` — **no mark on that option either** — then thirteen more questions
(degree, Brazil, recognition, experience, shortage, Dutch study, German,
English, age, stay, spouse, funds) to:

> **Nothing open on these answers.** Your 16 answers were compared against 23
> published rule sets. … · 0 OPEN · 0 WITHIN REACH · 23 NOT YET — 2 HAVE AN
> OPEN UNKNOWN · GERMANY 8 not yet — 1 has an open unknown · **FRANCE 5 not
> yet** · SPAIN 4 not yet · NETHERLANDS 6 not yet — 1 has an open unknown

The France section, opened: the same five "Needs a job offer" cards and nothing
else; the strings *chercheur*, *researcher*, *talent-researcher* and *research
hosting* appear **nowhere** on the page. **User cost:** B1's cost, sixteen
questions in instead of two, for the reader the one-pager describes as
"looking at DE/FR/ES/NL" — the person most likely to pick the fourth option.
The fix is the s19 sentence, rendered a second time: on question 3's `France`
option when the situation is research, and as the France section's summary
line ("France: its researcher route is quoted, not scored — read its rules")
in the four-country result.

**N2 — Friction. A returning reader with a finished record gets a poor layout
shift (0.56–0.76) and a masthead that moves, on the arrival the s10 gate
believes it measures.** *`cls-finished-before.png` / `cls-finished-after.png`,
390×844, module 700 ms late; table above.* **User cost:** every reader who
finished once and comes back — the field-CLS population s10 was written for —
watches the headline change under their eyes and the stamp jump; and the gate
that would catch a regression here is green on a seed that is not the
arrival. Fix in two parts: seed the s10 test with a record the product itself
writes (harvest one, or build it with the interview's real ids), and give the
finished-record arrival the same pre-paint decision the other four have (the
masthead and box are decided in `<head>`; a finished record can be recognised
there as surely as a half one).

**Polish, seen on the new screens.**

- The Q2 mark is a sibling `<small>` with no `aria-describedby` from the
  option: a keyboard or screen-reader user who Tabs to "I have (or expect) a
  hosting agreement…" hears nothing about "not scored" — the reader B1 is
  about is the one this mark does not reach in focus mode.
- "read it here ↗" — the external-link glyph on an internal link
  (`/france/talent-researcher/`); the same link in the result subline ("read
  its rules") has no glyph, and the footer uses ↗ only for GitHub and the
  licence.
- After a correction the new verdict is announced but focus is on `<body>`
  (the F6 fix applies to the interview, not to the re-answer).
- The new route page carries the old F3 line verbatim: "a daily check re-reads
  every source; the last run did not reach one of them" — on a page whose every
  source reads 2026-09-16.

### Rubric — PRODUCT_RUBRIC 1.3, v1 / v1.1 / v1.1-after

Same rubric version on all three columns; the deltas are subtracted. Only
lenses whose evidence changed move; each unchanged lens says why.

| Lens | v1 (09-08) | v1.1 (09-16) | v1.1-after (09-16) | Δ | Why |
|---|---|---|---|---|---|
| 1 First-run clarity | 4 | 4 | 4 | 0 | Not re-walked beyond question 1; the one contradiction (P6, "A few quick questions." over "UP TO 24") is still on the first screen (`resume-390.png`). |
| 2 Flow friction | 3 | 3 | **4** | **+1** | B1 and B2 cleared by operation; F5 (silent resume) cleared — "Continuing where you left off — 3 answers kept. Start over"; F6 cleared. Held from 5 by N1 (the same eliminating answer still leads nowhere on the `all` path, sixteen questions in) and F2 (the ladder, not re-walked, not touched). |
| 3 Copy & framing | 3 | 3 | 3 | 0 | F1 and P1–P4 were not in scope and were not touched; P4's "quoted and dated · scored, one in our own reading, not asked" is on every card of the new France screen. The new sentences are good ("No scored route in France takes a research hosting agreement.") but the lens's backing findings stand. |
| 4 Trust surfacing | 4 | 4 | 4 | 0 | F8 cleared ("Asked in the interview — you declared…") — one of the two things holding this off 5; the other, F3, is now on the new route page too, verbatim. Held at 4. |
| 5 Result actionability | 3 | 3 | 3 | 0 | F1 and F4 not in scope, not touched. The France dead end now has a next action ("read its rules") — credited under Edge states, where the finding lived. |
| 6 Edge states | 4 | 3 | **4** | **+1** | The state that dropped this lens is designed now: a written headline, a subline naming the route, a link to a real quoted page, at both widths. Held from 5 by N1 — the identical reader on the four-country path meets the undesigned state — and F7 (not re-walked). |
| 7 Accessibility basics | 4 | 4 | **5** | **+1** | F6 cleared: focus lands on the next question's first option (or the passport input) after every answer, and on the corrected question's first option after a `✎`; the new verdict is announced by the polite region. Ring, Escape, live regions as before. The two residues (focus to `<body>` after a re-answer, the Q2 mark not described to its option) are polish, listed above. |
| 8 Responsive | 4 | 3 | **4** | **+1** | B2 cleared at 390×844 and 390×1400: card top 0, first option focused, headline at 49 px after the re-answer. Held from 5 by P10 (each question still starts ~445 px down at 390 — `b2-390-after-q3-focus.png`), unchanged and not in scope. |
| 9 Design fidelity | 4 | 4 | 4 | 0 | Not re-walked for fidelity; the new screens match the tokens in the screenshots taken (display serif headline, small grey mark, one link colour). One new slip (↗ on an internal link) joins P5/P8. |
| 10 Orientation | 4 | 4 | 4 | 0 | Not re-walked; the new route page has the one header and footer, "Also in France", and is reachable from `/france/`, question 2 and the result. P9 (`/data/` names open JSON) not touched. |
| **Total** | **36** | **35** | **39** | **+4** | Four lenses rose, each on a finding cleared by operation; none fell. |

### Adoption verdict — the researcher persona

**P3 — Rafael, France, direct path.** *Would finish (two taps), would read the
route page, would come back once the route is scored, would recommend.* He is
told, in the headline, that no scored route takes his agreement and, in the
next sentence, that his route exists here and where its rules are; the page
he lands on quotes the fiche's own conditions with the day they were read.
**What still costs him:** the green "0 OPEN" box under a headline that has
just explained why, and — for a screen-reader user — a mark he does not hear
on the option itself.

**P3 — Rafael, "Any of these four".** *Would not finish in any useful sense,
would not come back.* Sixteen questions to "Nothing open on these answers."
with France folded to "5 not yet" and no mention of the researcher permit
(N1). This is B1 on the path the one-pager's own phrasing ("looking at
DE/FR/ES/NL") points him to.

**P3′ — Rafael, Spain.** *Would finish in three taps, would recommend.* The
card now says the hosting agreement was asked and what he declared (F8
cleared); nothing on it reads as the product forgetting what he said.

### Gate reading

**B1 cleared, B2 cleared, both by operation on the live product.** One new
blocker, **N1**, is B1's shape on the four-country path — smaller in
surprise (the reader chose "show me everything"), larger in cost (sixteen
questions). One new friction, **N2**, is a measurement the s10 gate believed
it had and did not. Recommended pick, in order: **N1** (the s19 sentence on
question 3's France option and on the France summary line of the four-country
result — copy that already exists), then **N2** (re-seed the finished-record
row with a record the product writes; then decide the finished-record
masthead in `<head>` like the other four arrivals).

**Controls operated in this re-walk.** Q1 `de`, `fr`, `es`, `nl`, `all`; Q2
`offer`, `research`, and the questions that follow through to a result (twelve
answers, Germany; sixteen, all four; three, Spain); the Q2 mark's `read it
here` link (followed); the result subline's `read its rules` link (followed to
`/france/talent-researcher/`, whose text was read top to bottom); the passport
combobox (`Turk` → Türkiye, `Braz` → Brazil); the `You declared` `<details>`
(opened) and its `salary_eur_year` row (tapped, at three viewports);
`band_3` (picked, verdict recomputed); the resumed line's `Start over`
(clicked, storage verified empty); the four-country result's `France`
`<details>` (opened). **Not operated:** anything outside the two blockers'
paths — `/data/`, `/feedback/`, the header disclosure, the 404, the German and
Dutch results beyond the mark check.
