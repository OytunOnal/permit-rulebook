# Review — s36, the address the watch connects to

slice: s36
branch: s36-resolve-time (data and site)
base: a9e6277 (data master, s35's merge) · ab8b59d (site, the scenario's own commit)
head: (pending)
head-data: (pending)
date: 2026-09-24
axes: Standards · Spec · Security — four rounds so far, eleven reviews
records: anchors into the data repository are written `../permit-rulebook-data/<path> @ <data sha>`; the hygiene script counts them as outside this repository and the record's author verifies them. A closure whose fix lives there names the day of the DECISIONS gate row as well as the sibling commit (DECISIONS 2026-09-24).

Scenario: `docs/spine/scenarios/s36-the-address-the-watch-connects-to.md` (approved 2026-09-24, "onaylıyorum"; one dated correction paragraph from the build at `e84c6b6`).

The runner read the branch once before the review (run 36034026797, `commit=false`, `dispatch=false`, job green in 158 s): 46 sources, all read, `watch complete … unreachable: 0, lapsed: 0, outages: 0, refused: 0` — the fetch tier's whole rewrite, read against the live sources.

## Open lines

open with reason: ../permit-rulebook-data/src/watch/request.ts:164-174 @ 72b84f8 — decompression is synchronous, uncapped, and runs after the budget's timer is cleared: `gunzipSync`/`brotliDecompressSync`/`inflateSync` take no `maxOutputLength` (default ~4 GiB) and run on the event loop one line after `clearTimeout(timer)`. Before this slice `fetch` decompressed as a stream under `signal: AbortSignal.timeout(BUDGET_MS)`, so size and time were both bounded — the slice removed a guard. A few KB of `content-encoding: gzip`/`br` from any of the 46 sources, or from any redirect target a `link` entry reaches under `anywhere`, allocates gigabytes synchronously and stalls or kills the run. No live instance (no source sends a bomb today; every source travels this path daily). Not braked: it is a Security finding in this change's own hunks and the fix restores what the change removed — rule 5's exception, closed in delta 1 with its round.
closed: ../permit-rulebook-data/src/watch/request.ts:164-174 — every decoder path bounded by `MOST_OF_A_BODY` (16 MiB, measured: the largest decompressed body in the watchlist today is 1,040,895 bytes), `maxOutputLength` failing rather than truncating, the zlib calls async so the event loop is never held, and the budget's timer no longer released before the page exists (`request.ts:156-167, 245-250, 264-268 @ 3db97b3`, verified by the delta round's Security axis); data 4e09b08 (the sibling repository's commit; this repository verifies the day instead — gate row 2026-09-24)
open with reason: ../permit-rulebook-data/src/watch/request.ts:197,270-271 @ 3db97b3 — the bound covers the decoders, not the bytes: nothing counts what accumulates, so a source answering un-encoded (or with an encoding the switch does not know) streams whatever it can push inside `msLeft` into `parts`, `Buffer.concat` doubles it, and the identity path returns it with `MOST_OF_A_BODY` never consulted. As true before this slice (`fetch`'s `arrayBuffer()` buffered without a cap) as after, but this is the slice that declares the bound. A hostile source, or any redirect target a `link` entry reaches under `anywhere`, OOMs the runner. Live instance: yes — the path every source travels. Security, delta round 1; fixed in delta 2 with its round.
closed: ../permit-rulebook-data/src/watch/request.ts:197,270-271 — the gate counts what arrives, before any push: `arrived += chunk.byteLength; if (arrived > MOST_OF_A_BODY) { parts.length = 0; failed(pastTheBound("on the wire")); req.destroy(); }` (`../permit-rulebook-data/src/watch/request.ts:317-327 @ c67ad01`), the only `data` handler and the only `parts.push` in `src/watch`, no `setEncoding` so a chunk is a Buffer, overshoot one chunk, the settle single and the timer idempotent — verified by the delta round's Security axis; measured cost on a 16 MiB body 68.6 ms without the gate, 66.9 ms with; data 13fe1eb (the sibling repository's commit; this repository verifies the day instead — gate row 2026-09-24)
open with reason: ../permit-rulebook-data/src/watch/fetch-source.ts:677 @ c67ad01 — the fetch tier's thrown text goes into the log line, the flag file and the issue unbounded and unsanitised (`error: afterAnswer(code ? `${String(e)} (${code})` : String(e), answerStatus)`), while `shortFailure`/`MOST_OF_A_FAILURE` exists for exactly that and is applied only on the browser tier (`cdp.ts:408`) — and this change's own sentence in `failure.ts:116-118` says the message travels into all three. An outsider's control over its length is about a redirect-chosen hostname, hence soft. No live instance. Security, delta round 2; closes in delta 3 with one owner for both tiers.
closed: ../permit-rulebook-data/src/watch/fetch-source.ts:677 — `const said = shortFailure(String(e))` with the code and the status appended after the bound (`../permit-rulebook-data/src/watch/fetch-source.ts:705,709 @ f80bef6`); the tier's only other exit is the `ReadFailure` branch, whose two construction sites carry our own words and a `shortAddress`-bounded name, so no source text is unbounded; worst case 259 characters and no consumer assumes 200 — verified by the delta round's Security axis; data 372fb25 (the sibling repository's commit; this repository verifies the day instead — gate row 2026-09-24)
open with reason: ../permit-rulebook-data/src/watch/fetch-source.ts:705 @ f80bef6 — the bound cuts the sentence's length, not its content: newlines and control bytes inside the first 200 characters still reach the log line (JSON, escaped there), the flag file and the issue body (Markdown, where they are not), and no sanitiser exists in `src/` — `core.ts:407` only names one in a comment. The same rule is unapplied on the browser tier's own return path (`browser.ts:153` returns `e.message` raw) while this change's comment says the rule has one owner for both tiers. Reach: a source's thrown message. No live instance. Security, delta round 3; closes in delta 4 with one owner that cuts and makes printable, applied on both tiers.

## Round on 72b84f8 (data) · e84c6b6 (site)

### Security

**Hard 1 — a source can inflate the run to death: decompression is synchronous, uncapped, and deliberately released from the budget before it runs.** `../permit-rulebook-data/src/watch/request.ts:164-174 @ 72b84f8`:

```
164          bytes: () => new Promise<Uint8Array>((whole, no) => {
166            const parts: Buffer[] = [];
167            res.on("data", (chunk: Buffer) => { parts.push(chunk); });
169            res.on("end", () => {
170              clearTimeout(timer);
171              try { whole(unpacked(Buffer.concat(parts), oneHeader(res.headers["content-encoding"]))); }
```

and `request.ts:103-113`: `case "gzip": case "x-gzip": return new Uint8Array(gunzipSync(bytes));` · `case "br": return new Uint8Array(brotliDecompressSync(bytes));`

What an outsider does: any of the 46 sources, or — under `anywhere` — any redirect target a `link` entry reaches, answers a few KB with `content-encoding: br` (decompressed though never asked for, by the file's own admission) or `gzip`. No `maxOutputLength`, so the default is `buffer.kMaxLength`: up to ~4 GiB allocated synchronously, on the event loop, with the budget timer already cleared one line above. Two guards that existed before are gone: `a9e6277:src/watch/fetch-source.ts:277,312` passed `signal: until` into `fetch`, and undici decompressed as a stream under that signal. Nothing now bounds either the inflated size or the time spent producing it — and a blocked event loop could not fire the timer even if it were still armed. Effect: the watch process OOMs or stalls, the pass dies, no source is reported. Live instance: none (no watchlist source sends a bomb today; every source and every `anywhere` hop can).

**Checked, not findings.** No socket skips the hook: `lookup` survives the explicit agent (`fetch-source.ts:475,511-516`), keep-alive sockets are keyed host:port so a cross-host hop re-resolves, the pools are destroyed per source (`:598-601`), IP literals bypass `lookup` by design and are judged where spelled (`:455`, `refusedTarget:225-236`), and an explicit agent bypasses Node's env-proxy agent so `NODE_USE_ENV_PROXY` cannot divert. `all: true` is handled: `floorLookup:305-315` judges every returned address before returning the list, so happy-eyeballs can only pick a judged one; the `all: false` branch returns a judged `addresses[0]`. Printed strings: `thrownAs:77-80` keeps the `TypeError: fetch failed` + `cause` shape, `causeCode` prints only a code of fixed shape, every address in a refusal goes through `shortAddress`, and `refusedResolution:252-261` names the kind, never the address. The budget across hops holds (`:465,515` one deadline, `msLeft` per hop, `request.ts:145` destroys mid-body) — except for the inflate above. TLS untouched: no `rejectUnauthorized`, the hostname (not the address) reaches `https.request` (`request.ts:123`), so SNI and the certificate name stay the name; non-`http(s)` and credentialled targets still refused. `threats.md` has no row for "the watch is made to request an address nobody meant" — this diff closes it and the model still does not list it (outside the delta; a note). s35's Dismissed list is all `core.ts`, untouched here.

1 finding (1 hard, 0 soft).

### Spec

All eight points implemented; five of six proof bullets proved by a test, one partially. The site diff is 33 added lines, zero deletions — the eight points and "How it is proved" byte-identical, the correction paragraph the only change.

**Soft 1 — a fourth claim the correction paragraph did not correct.** The scenario (`docs/spine/scenarios/s36-…md:146-147 @ e84c6b6`): "Not a wider refusal than today's rule: public stays public, and an entry on a private or loopback address may still be read where it already is." `../permit-rulebook-data/src/watch/fetch-source.ts:466 @ 72b84f8` builds the hook with the entry's **name** as baseline (`floorLookup(entry.hostname, resolver)`) and `allowedAddress` (`:216`) compares `going === addressKind(entryHost)` — for a name, `addressKind` is `public`. So an entry spelled as a name that resolves to loopback or private is now refused, where today it is read. Intended by point 1 (the `mirror.example.org` → `10.0.0.5` case), but the line was left standing. Live instance: none.

**Soft 2 — the doc point 8 asked for repeats the claim the build corrected.** `../permit-rulebook-data/CONTRIBUTING.md:265-268 @ 72b84f8`: "classifies every one of them, and hands the connection **the single address it approved**", which the correction paragraph retires and `floorLookup:312` contradicts (`if (options.all) { done(null, addresses); return; }`). Live instance: yes (the file ships).

**Soft 3 — the four prefixes are proved from a loopback entry, not a public one.** The proof bullet says "from a public entry"; `tests/s36.test.ts:114-124` uses `fixtureOrigin = http://127.0.0.1:${port}`. Same outcome, but the entry kind the bullet names is never exercised. Live instance: none.

**Soft 4 — the new entry-literal refusal has no test.** `fetch-source.ts:455-460` refuses a link-local *entry*; it follows points 4 and 2, so not scope creep, but no case covers it. Live instance: none.

**Soft 5 — a resolve-time refusal on a hop loses the status.** `fetch-source.ts:612` returns no `status`, while the same hop refused from the `location` header returns `status: res.status` (`:548`) — s34 pinned that the source's own answer is reported (`tests/s34-fetcher.test.ts:155`). Point 5 says "same errors". Live instance: none.

**Soft 6 — decompression arrived through the correction, not a point.** `request.ts:96-108`, including `br`, which `accept-encoding: gzip, deflate` never asks for. Justified by keeping the fourth `fetch` header, but it is a new responsibility no numbered point carries; the scenario's own fork line (`:142-145`) is where the cost of the rewrite belongs — 179 new lines and 514 changed re-implementing headers, body assembly, decompression, the error wrapper and the socket budget, plus per-source pools that no longer keep sockets warm between sources, with the dispatched run the only evidence that the wire behaviour held.

Nothing else unasked; no test asserts content in place of a decision — the zero-request counts and `resolutions - startedAt === 1` are decisions, and the corrected second-call pair is built both ways round. 6 findings (0 hard).

### Standards

**Hard: none.**

**Judgement 1 — the retired sentence, in shipped code and in the contract.** `../permit-rulebook-data/src/watch/browser.ts:26-29 @ 72b84f8` ("hands the socket the one it approved") and `CONTRIBUTING.md:263-265` repeat what the build's own correction paragraph retires and `floorLookup:312` contradicts. standards.md, *Naming and shape*: "a comment that describes a retired rule is a finding". Live instance: yes (both files ship).

**Judgement 2 — a header that names the tier's gates and not its gap.** `browser.ts:31-36`: "the same two gates it has always had: the entry address is refused before Chrome is asked (`refusedAddress`)" — `refusedAddress` tests credentials, malformed and scheme only; the entry *kind* check is `fetch-source.ts:455`, which this tier never calls, so a browser entry spelled `http://169.254.169.254/` passes both named gates. The gap is untouched by this diff (Security's to grade); the header is the place that claims the gates. Same hunk: "The seven browser entries" names no source. Live instance: yes.

**Judgement 3 — a comment describing a design the code does not have.** `request.ts:138-143` says the timer "is cleared on the request's `close`"; the code clears it at `:150`, `:152` and `:170`. Live instance: yes.

**Judgement 4 — one docstring, four subjects.** `fetch-source.ts:479-510` over `ask()`, its last two subjects now duplicated by `ASKING`'s own docstring at `:397-404`. Live instance: yes.

**Judgement 5 — Duplicated Code.** "an error travels into a log line, a flag file and the issue that flag becomes" written out three times (`request.ts:68-72`, `fetch-source.ts:249-251`, `:620-622`). Live instance: yes.

**Judgement 6 — Shotgun Surgery.** The asked-for encodings (`fetch-source.ts:403`) and the unpacked set (`request.ts:105-112`) must change together; `unpacked` cites the header but not the constant. Live instance: yes.

**Judgement 7 — a number that names no source.** `tests/s36.test.ts:178`: `expect(Date.now() - started, …).toBeLessThan(1_000)` — wall-clock, unsourced. Live instance: yes.

Looked for and not found: the hop loop, the header set and the error mapping are each in one file only — no duplication between `request.ts` and `fetch-source.ts`; the test assertions key to `AddressKind`, a set `CONTRIBUTING.md:266-270` declares, so the substring checks are within the standards' *Tests* exception; no source-grep test; the site's +33 is the permitted dated form, appended only. 7 findings, 0 hard.

### The round's sort

Behaviour, Security, in this change's own hunks — fixed, and that delta takes a round: the uncapped synchronous inflate outside the budget (the open line above). Not braked: rule 5's exception carries a Security finding in the change's own hunks, and the fix restores a guard this change removed rather than adding a mechanism.
Convention, taken in the same delta: Standards 1–7 and Spec 2 (the retired sentence in `browser.ts` and `CONTRIBUTING.md`), Spec 3 (the prefixes from a public-named entry), Spec 4 (the link-local entry's case), Spec 5 (the refused hop's `status`), Spec 1 (the correction paragraph says which line governs the widened refusal for name entries).
Recorded, not built: Spec 6 — the cost of the rewrite. The scenario's own fork line says a cost larger than the hole is the human's; the numbers are in the round above and in the build's report, and the session puts them to the human at the change's close rather than opening a fifth round about them.

## Delta round 1 on 3db97b3 (data) · b91e829 (site)

The delta (data 72b84f8..3db97b3, nine commits; site 467d7e2..b91e829, the correction paragraph): the bound and the budget, `ENCODINGS_ASKED_FOR`, the docstrings, the browser tier's header, the link-local entry cases, the refusal that reports the source's status, the refusal proved by what it says. Gates at 3db97b3 (the builder): 46 files / 830 tests, `npm run check` green. The runner read the delta (run 36039514032, `commit=false`, job green): 46 sources, all read, `watch complete … unreachable: 0, lapsed: 0, outages: 0, refused: 0`.

### Security

**Prior open line: closed.** `request.ts:156-167, 245-250, 264-268 @ 3db97b3`: `unpacking` passes `{ maxOutputLength: MOST_OF_A_BODY }` to every decoder, `ERR_BUFFER_TOO_LARGE` becomes `pastTheBound()`, the raw-deflate fallback rethrows rather than inflating twice, `maxOutputLength` errors and never truncates, and the timer is released only where nothing is left to bound (four exits, each verified; `reading` is set before `close` can fire). The per-source budget still covers a pool-queued unpack.

**Hard 1 — the bound covers the decoders, not the bytes.** `request.ts:197, 270-271`: `default: return Promise.resolve(new Uint8Array(bytes));` over `const parts: Buffer[] = []; res.on("data", (chunk) => { parts.push(chunk); });` — nothing counts the accumulation, so an un-encoded body is unbounded and `Buffer.concat` doubles it. Equally true before this slice; this is the slice that declares the bound. Live instance: yes.

**Hard 2, ruled not a finding by the session — the scenario's correction paragraph.** The axis read the site hunk as an edit to a held-out acceptance scenario. This project licenses exactly one dated `**Corrected …, by the build:**` paragraph, which may shrink or replace and never grow; the session verified the held-out prefix (lines 1–147, the eight points and the proof list) is byte-identical at `ab8b59d`, `e84c6b6` and `b91e829` (sha256 prefix `8eaf0c90d6c55185` at all three) and the paragraph shrank 2,147 → 2,141 bytes. No finding.

**Soft 1 — the thread-pool test can hang CI.** `tests/s36.test.ts:366-368`: `pbkdf2("hold the pool", "s36", 2_000_000, …)` scales with `UV_THREADPOOL_SIZE` and the `finally` awaits every `busy`; on a 2-vCPU runner it can outlast the default test timeout. A flake, not a hole. Live instance: yes.

### Spec

**Hard: none.** Every sort item present: Spec 2 (`CONTRIBUTING.md:264-270`, `browser.ts:26-56`), Spec 3 (corrected in the paragraph), Spec 4 (`tests/s36.test.ts:162-176`), Spec 5 (`fetch-source.ts:531,630`), Spec 1 (the paragraph). Points 1–8 and "How it is proved" byte-identical.

**Soft 1 — Spec 5 was closed into a field no consumer reads.** `core.ts:439-441`: the `unreachable` report drops `status`, so the `status: 200`/`302` the delta added reaches no log line, flag file or issue — observable only from a test. Consistent with s34's pin, but the consistency bought is internal. Live instance: yes.

**Soft 2 — the principle applied to one of two throw branches.** `fetch-source.ts:649-653` (a socket reset or the budget spent after a 200) returns no `status` while `answerStatus` is in scope, against `:459-466`'s own sentence. Live instance: none.

**Soft 3 — the correction paragraph names the wrong governing line:** "Point 1 governs" for the widened refusal of a name entry, where the widening comes from point 2's relative rule. Live instance: yes.

**Soft 4 — "reads the next source anyway" is proved one call below the pass:** `tests/s36.test.ts:332,346` re-call `fetchSource`, while the regression guarded (one oversize body killing the pass) lives at `core.ts:439`. Live instance: none.

Checked, not findings: `refused-by-source` + 200 fits the classes (answered, not a page, not retried); the slow-unpack case fills exactly `UV_THREADPOOL_SIZE` and its flake direction is red, not false green; the link-local entry case is demanding; the public-entry-over-loopback pair is unconstructible under the relative rule, so the bullet's replacement is correct and outcome-identical; `MOST_OF_A_BODY`'s "44 addressable" reconciles (46 − 2 `human`).

### Standards

**Hard: none.** Prior findings 1, 2, 3, 4, 7 closed; 5 closed at its chosen home (`failure.ts`'s module header), `core.ts:405` accepted as a different subject.

**Judgement 1 — the bound's justification is wrong against this repo's own Node.** `request.ts:118-133`: `buffer.kMaxLength` on Node v24.20.0 is 9007199254740991 (~8 PiB), not ~4 GiB, so "256 times smaller than zlib's default" is false by ~2 million ×. The measurement half is sourced and verifies (16 MiB / 1,040,895 = 16.1×). Live instance: yes.

**Judgement 2 — `ENCODINGS_ASKED_FOR` owns the header, not the set.** `request.ts:135-145` claims what is asked for cannot drift from what is unpacked, while `unpacked` (`:180-197`) still switches on its own literals. Live instance: yes.

**Judgement 3 — `failure.ts:116-117`**: "the message is one of the three places this file opens by naming" — the three are the log line, the flag file and the issue; the message is the text's source, not a destination. Live instance: yes.

**Judgement 4 — `tests/s36.test.ts:377-384`**: `pbkdf2(…, 2_000_000, …)` and `msLeft: 150` name no source and the docstring's "about a second" is unmeasured; the `UV_THREADPOOL_SIZE` assumption is stated and the case asserts a decision. Live instance: yes.

**Judgement 5 — the correction paragraph** rewrote three recorded corrections and dropped a measured detail (`{ hints: 0, all: true }` → `{ all: true }`). Live instance: yes.

### The delta round's sort

Behaviour, Security, live instance — fixed, and that delta takes a round: the unbounded accumulation (the open line above).
Convention, taken in the same delta: Standards 1–5, Security soft 1 (the test's bound and its sources), Spec 1 (the status carried to where a curator reads it, or the comment saying why it stays internal), Spec 2 (both throw branches alike), Spec 3 (the governing line), Spec 4 (the guard proved through `runWatch`).
Ruled not a finding: Security hard 2 — see above.

## Delta round 2 on c67ad01 (data) · d3007f5 (site)

The delta (data 3db97b3..c67ad01, nine commits; site 00edc55..d3007f5, the correction paragraph 2,141 → 2,130 bytes): the byte gate, the bound's justification against this Node's real `kMaxLength`, the encodings owner, `failure.ts`'s sentence, the second throw branch's status, the pass guard through `runWatch`, `afterAnswer`, the pool case bounded and sourced, the refusal naming where the bound was passed. Gates at c67ad01 (the builder): 46 files / 836 tests, `npm run check` green, 252.80 s with a real Chrome.

### Security

**Prior open line: closed** (`../permit-rulebook-data/src/watch/request.ts:317-327 @ c67ad01`) — see the closure above; the axis walked the handler, the settle, the timer and the overshoot, and read the exactly-at-bound case as whole.

**Soft 1 — an unbounded printed string, on a line this delta wrote.** `../permit-rulebook-data/src/watch/fetch-source.ts:677 @ c67ad01`: `afterAnswer`'s own addition is a number (`res.statusCode ?? 0`), but the base `String(e)` goes unbounded and unsanitised into the log line, the flag file and the issue, while `shortFailure`/`MOST_OF_A_FAILURE` is applied only at `cdp.ts:408`. Live instance: none.

Checked, no finding: `pastTheBound`'s text names only our constant and the measurement site; `discard: () => res.resume()` drains without buffering under the budget; the pool case is bounded with an explicit timeout; the scenario prefix byte-identical either side, all sixteen changed lines inside the correction block.

### Spec

**Hard: none.** Every sort item lands: Spec 1 both halves (`fetch-source.ts:427-438, 649, 676-677`; `core.ts:438-446`), Spec 2 (`:674-678`), Spec 3 (the paragraph, "Point 2's relative rule governs, not point 1"), Spec 4 (`tests/s36.test.ts:512-541`). The class holds on both new shapes and the exactly-at-bound body reads whole (`request.ts:319` against `tests/s36.test.ts:427-434`).

**Soft 1 — the pass guard lost the shape it came from.** The "next source reads anyway" half was deleted from the *unpacked* case (`tests/s36.test.ts:387-399`) when the `runWatch` guard was added for the wire case; a decoder refusal killing the pass now fails nothing. Live instance: yes.

**Soft 2 — an unproved distinction that reaches a curator.** `request.ts:133-139`, `pastTheBound(measured: "unpacked" | "on the wire")`: nothing asserts either word, so the wire path tagged "unpacked" would tell a curator a page inflated when it is simply that big. Live instance: yes.

**Soft 3 — the clause lands on a subordinate clause:** "…, which is no page this watch reads, after HTTP 200". Live instance: yes.

**Soft 4 — the encodings case proves a compile-time truth at runtime:** `ENCODINGS_ASKED_FOR`'s members are `keyof typeof UNPACKINGS`, so the drift it names is already a build failure. Live instance: yes.

`parts.length = 0` is inside the needed mechanism — no finding.

### Standards

**Prior five closed** (the `kMaxLength` figure measured and the ratio checked; `UNPACKINGS` the owner with `keyof` forbidding any non-key spelling; `failure.ts`'s sentence; the pool case's machine and numbers dated, `ubuntu-24.04` matching the workflow, the timeout ≈ 39× the measurement; the paragraph's measured form restored). **Hard: none.**

**J1** — `fetch-source.ts:423-441`: `/** 2xx and nothing else… */` documents `answered` but now sits above `afterAnswer`. **J2** — `:427-428`: "The structured `status` … is read by this file's own callers" is untrue; `core.ts` declares the field and never reads it, only tests do. **J3** — `core.ts:441-446`: "the browser tier has no status to give" is untrue of the tier (`cdp.ts:430` throws `servedFailure(served.status, …)`), true of `browser.ts`'s return shape. **J4** — the site paragraph: `Node v24.20.0` became `v24.20.0`, a version naming nothing. All four live.

### The delta round's sort

Security, in this change's own hunks — closes in delta 3 with its round: the unbounded thrown text, one owner for both tiers (the open line above).
Convention, taken in the same delta: Spec 1 (the pass guard restored for the encoded shape), Spec 2 (the `measured` words pinned or dropped), Spec 3 (the sentence reads as a sentence), Spec 4 (the encodings case proves something runtime or goes), Standards J1–J4.

## Delta round 3 on f80bef6 (data) · 1b2fda6 (site)

The delta (data c67ad01..f80bef6, two commits; site the correction paragraph, 2,130 → 2,130 bytes, the version restored inside): the thrown text bounded where it is printed, `core.ts`'s comment made true of the reader rather than the tier. Gates at f80bef6 (the builder): 46 files / 838 tests, `npm run check` green. The runner read the final code (run 36045642430, `commit=false`, job green in 174 s): 46 sources, all read, nothing unreachable.

### Security

**Prior open line: closed** (`../permit-rulebook-data/src/watch/fetch-source.ts:705,709 @ f80bef6`) — see the closure above. The axis walked the catch's two exits, the `ReadFailure` construction sites (`fetch-source.ts:324` `refusedResolution`, text bounded by `shortAddress`; `request.ts:134` `pastTheBound`, a constant), the worst-case length (200 + `" ("` + 40 + `")"` + `"after HTTP 999, "` = 259) and the appended parts' alphabets (`A_CODE`, a status number): no injection, no unbounded source text, nothing downstream assuming 200.

**Soft 1 — the bound cuts length, not content.** `fetch-source.ts:705`: newlines and control bytes inside the first 200 characters still travel to the flag file and the issue body; no sanitiser exists in `src/`. Not opened by this delta — the length hole is what this change made and closed. Live instance: none.

### Spec

**Hard: none.** Spec 1–4 all land: the encoded pass guard really runs through `runWatch` with a second entry read and snapshotted (`tests/s36.test.ts:584-607`); the bound's case asserts the composite (`MOST_OF_A_FAILURE + 59` against the whole error), not the prefix; nothing asked-for missing, nothing unasked landed.

**Soft 1 — the `measured` distinction is pinned as phrasing.** `tests/s36.test.ts:409, 439-441, 584-585, 601`: which branch fired is a decision and the case is substantively right, but the test retypes the literals instead of naming the union the contract declares (`request.ts:133`), so rewording `pastTheBound` fails four assertions with nothing wrong. Live instance: yes.

**Soft 2 — the runtime encodings case never proves the request it reads is its own.** `tests/s36.test.ts:369-371`: the result is discarded and nothing asserts the fetch answered, so a failure before `ask` kills the case on `asked.at(-1)!` rather than on its own message. Live instance: yes.

### Standards

**J1–J4 all closed** (the `res.ok` one-liner back above `answered`; the docstring saying `core.ts` declares the field and never reads it; `core.ts`'s sentence true of the browser *reader*'s shape, with `cdp.ts`'s `servedFailure` named as where the tier spends its status; the site paragraph's `Node v24.20.0` in both places). **Hard: none.**

**Soft J5 — a number sourced by prose.** `tests/s36.test.ts:558`: `" (".length + 40 + ")".length`, where `40` is `A_CODE`'s limit in `failure.ts:143`, unexported and held together only by a comment. Live instance: yes.

**Outside the delta.** `browser.ts:153` returns `e.message` unbounded — the "one owner for both tiers" rule this change's own comment states is not applied on the browser return path. Folded into the open line above, because it is that rule's other half.

### The delta round's sort

Security, the change's own rule — closes in delta 4 with its round: one owner that cuts and makes printable, applied on both tiers (the open line above).
Convention, taken in the same delta: Spec 1 (the two words named from the contract), Spec 2 (the encodings case asserts its own read first), Standards J5 (the code's limit exported and used).

## What real-green means here

The first scheduled run on data master after the merge reads all 46 through the new connect path with no `unreachable` among them, and the site's next pin lands — the fetch tier's rewrite is what is being proved, and the runner is the only place it runs against the live sources. The readings of 2026-10-09 are not this slice's.

## Gates at the round's head

Data `npm run check` at `72b84f8` (the builder, 2026-09-24): 46 files / 824 tests, build, typecheck:tests, validate, watch:coverage green, 253.77 s with a real Chrome. Runner: run 36034026797 as above (46/46, 158 s).
