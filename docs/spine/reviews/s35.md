# Review — s35, a source that fails once is read again

slice: s35
branch: s35-retry (data and site)
base: b4ddd68 (data master, the runner pin) · c64ecdf (site master, the approval)
head: (pending)
head-data: (pending)
date: 2026-09-24
axes: Standards · Spec · Security — two rounds so far, six reviews
records: anchors into the data repository are written `../permit-rulebook-data/<path> @ <data sha>`; the hygiene script counts them as outside this repository and the record's author verifies them (Spine, 2026-09-24).

Scenario: `docs/spine/scenarios/s35-a-source-that-fails-once-is-read-again.md` (approved 2026-09-24, "onaylıyorum"; one dated correction paragraph from the build at `ceb8c65`).

The runner read the branch once before the review (run 36005247129, `commit=false`, `dispatch=false`, job green in 172 s): 46 sources, 45 read, `wetten-wav-artikel-8` `HTTP 403` → `refused-by-source` → a lapse (`since: 2026-09-24`, `warn`), `watch complete … unreachable: 1, lapsed: 1, outages: 0, refused: 0`, exit 0. The seven browser entries 6.7–29.8 s.

## Open lines

open with reason: ../permit-rulebook-data/src/watch/core.ts:561-570 @ e5425f0 — `verdictOf` tests today's unread against yesterday's list alone, so a source that fails on alternating runs is a lapse every time and never reddens a run; on its good day it drops off `unread` and its `since` with it, and nothing accumulates. The class is the source's to choose (`429`/`5xx` transient, other `4xx` refused-by-source — both green on day one); only refused-by-us is red at once. No live instance of the alternation; the flakiness is real (bamf.de: unread 09-21, read 09-22, unread 09-23 and 09-24). Braked — a behaviour finding on running code with no live instance; the human's word is asked on STATUS.
closed: ../permit-rulebook-data/src/watch/core.ts:561-570 — the human's word "düzelt" (gate row 2026-09-24): the state keeps each source's silent mornings (`lapses`), pruned past the week, and the run is red on two in seven; traced fail/ok/fail → red on the third run by the delta round's Security axis; data 5c82d47
open with reason: ../permit-rulebook-data/src/watch/failure.ts:88-92 @ e5425f0 — `causeCode` returns any string it finds on `e.cause.code`, unbounded, into the public run log; the repo bounds every printed address at 200 characters and this is the one printed thing with no bound. No attacker-controlled path found (Node and undici own `cause.code`); no live instance. Judgement, on the Security axis: closes in the delta with a bound of fixed shape.
closed: ../permit-rulebook-data/src/watch/failure.ts:88-92 — `causeCode` accepts `/^[A-Z0-9_]{1,40}$/` and prints nothing otherwise; data ef36df1
open with reason: ../permit-rulebook-data/src/watch/cdp.ts:382-386 @ e5425f0 — a page's own exception `description` (source-controlled, stack included) becomes `ReadFailure`'s text and reaches `report.error`, logged raw; it does not reach a flag file or an issue. The string is pre-existing; the hunk moved. No live instance. Judgement, on the Security axis: closes in the delta with a bound.
closed: ../permit-rulebook-data/src/watch/cdp.ts:382-386 — `shortFailure` (total bound, `MOST_OF_A_FAILURE = 200`) at the throw that takes the page's string; data ef36df1 and 08792f1 (the bound moved off `ReadFailure`'s constructor, where it cut a 500-character step diagnosis a curator reads)
open with reason: ../permit-rulebook-data/src/watch/core.ts:263-265 @ e5425f0 — `failure` is optional on `WatchReport` while required on `FetchResult`; a report with no class falls to `lapsed` (green) and is skipped by the retry. Type-enforced for every in-repo producer; no live instance. Judgement, on the Security axis: closes in the delta — an unknown failure is red.
closed: ../permit-rulebook-data/src/watch/core.ts:263-265 — `WatchReport` is two arms, `failure` required on the unreachable one; `verdictOf` reads a missing class as red; data f98f09b
open with reason: ../permit-rulebook-data/src/watch/core.ts:596-598 @ 08792f1 — `lapses` is read from the state file with no check of its shape and printed (`cli-watch.ts:153-154`) with no bound: an array of any length and any strings reaches the public run log, and a non-array value throws in `days.filter` and kills the run against `readJson`'s own promise. Repo write required; no live instance. Judgement, on the Security axis: closes in delta 2 with validation at the boundary.
open with reason: ../permit-rulebook-data/src/watch/core.ts:577-581 @ 08792f1 — a day that does not parse is forgotten rather than refused, erasing a silent morning green-ward while the comment documents pruning as old-side only. Repo write required; no live instance. Judgement, on the Security axis: closes with the same boundary check.
open with reason: ../permit-rulebook-data/src/watch/cdp.ts:414 @ 08792f1 — `served.url`, the document's response address after a redirect, chosen by the source, is put into a `ReadFailure` text raw where every other printed address goes through `printableAddress`; the throw is s35's own hunk (data 6339635), named by the builder and by the delta round's Security axis as outside the delta. No live instance. Closes in delta 2.

## Round on e5425f0 (data) · ceb8c65 (site)

### Standards

No hard findings. The standards file exists, names the gate commands and carries the dated lint exemption (`standards.md:17-20`), so neither Spine-specific finding applies: the only scenario edit is the required `**Corrected 2026-09-24, by the build:**` paragraph (`docs/spine/scenarios/s35-…md:159 @ ceb8c65`), and no new check can be silenced by writing content.

**Soft 1 — a retired rule left standing in the text a human reads.** `.github/workflows/watch.yml:153-156` (untouched by the diff, made untrue by it):
```
- name: Fail the run if any source was unreachable
  if: steps.watch.outcome == 'failure'
  run: echo "::error::watch reported unreachable sources — see the Watch sources step"
```
The condition is right (it keys on the exit code, as `cli-watch.ts:180-187` claims), but the name and the error now misdescribe it: after this diff a run can carry `unreachable` sources and stay green. `standards.md:46-47` — "a comment that describes a retired rule is a finding." Live instance: yes — 2026-09-23/24 both failed on `bamf-*`; the next single-source lapse prints this line's opposite.

**Soft 2 — a check keyed to a string's shape.** `../permit-rulebook-data/tests/s34-fetcher.test.ts:367 @ e5425f0`:
```ts
expect(answer.error.length, "the cause's message was printed too").toBeLessThan(200);
```
`standards.md:52-54` — "A test asserts a decision, not content… a word count or a sentence's shape is a finding." This is the only guard on the invariant that the cause's *message* (which carries the address) never reaches a flag or an issue, and a short undici message passes it. The decision form is `expect(answer.error).not.toContain(sourceOrigin)`. Live instance: yes, the file.

**Soft 3 — glossary carrying implementation.** `CONTEXT.md:406-419, 426 @ ceb8c65`: "`408`, `425`, `429`, any `5xx`" and "recorded as `since` on the state's unread item" — a field name and a status list, not a meaning. Live instance: yes, the file.

**Judgement (baseline smells), 4:**
- *Duplicated Code* — the empty-body rule is classed twice: `fetch-source.ts:377-387` and `core.ts:432-439`, both `"transient"`.
- *Divergent Change* — `core.ts:555-644` adds `RunVerdict`/`UnreadNotice`/`RunSummary` + three exports; log shaping in the engine file `failure.ts` was split out to avoid.
- *Shotgun Surgery* — the three-class table restated verbatim in `failure.ts:19-32`, `CONTRIBUTING.md:257-262`, `CONTEXT.md:406-419`, the scenario, `fetch-source.ts:365-367`.
- *Speculative Generality* — `core.ts:612-613` `u.since ?? today` guards a state `unreadSince` never writes; `cli-watch.ts:188` spreads an object already fresh.

Findings: 7 (watch.yml 1 · s34-fetcher.test.ts 1 · CONTEXT.md 1 · core.ts 3 · fetch-source.ts 1 shared · cli-watch.ts 1). Worst: the workflow's "Fail the run if any source was unreachable" step name and error, which now say the opposite of the rule.

### Spec

**Hard 1. The CLI's exit rule and its last line are never executed by a test.** Spec, *How it is proved*: "a transient failure that fails twice is a lapse (`unread` with `since: today`, **exit 0 through the CLI's own rule**, `watch:lapse` at `warn`) … a refused-by-us failure is **exit 1 on its first day** … the **`watch complete` line** carries `lapsed` and `outages`."

`../permit-rulebook-data/src/watch/cli-watch.ts:186-187 @ e5425f0`:
```
log(verdict.red ? "error" : "info", "watch complete", { ...runSummary(reports, verdict) });
process.exit(verdict.red ? 1 : 0);
```
No test in the range imports, spawns or greps `cli-watch.ts` (grep over `tests/`: 3 hits, all prose in comments; 0 executions). `tests/s35.test.ts:237,266,388` assert `verdict.red` and `runSummary(...)` — the core's return values. The line that turns them into the alarm, and the strings `watch:lapse`/`watch:outage`/`watch:refused` (built at `cli-watch.ts:150-152`), are unproved. This is the one behaviour the slice changes for a curator (`unreachable > 0 ? 1 : 0` → `verdict.red`). The build's correction paragraph shrank three other proof claims and left this one standing. **Live instance: yes** — `watch/state.json` lists `bamf-hochschulabsolvent` and `bamf-selbstaendige-taetigkeit` unread with no `since`; the next scheduled run reads both as outages and exits through exactly this untested line. Consumer/regression: the *watch run failed* issue opens or fails to open on a wrong exit code, and no test catches it.

**Soft 2. The retry is proved only into `baseline`.** Spec: "reports `unchanged`/`changed`/`baseline`, not `unreachable`". `tests/s35.test.ts:115,129,150,189` all run against `emptyState` — every retry-success case lands on `baseline`. No case has a prior snapshot, so "a retried read that matches yesterday reports `unchanged` and does not move `retrieved_at`" is unproved. Live instance: none.

**Soft 3. `refused` is a third field the spec did not ask for.** Spec point 5: "The `watch complete` line grows `lapsed` and `outages` beside `unreachable`." `core.ts:RunSummary` adds `refused`, and `cli-watch.ts:139` adds `failure:` to every report line. Additive, and it makes the buckets sum to `unreachable` — defensible, but it is growth, and the correction paragraph does not record it. Live instance: yes (every run's last line).

**Soft 4. A processing throw is classed `refused-by-source`, a class the spec never assigns it.** `../permit-rulebook-data/src/watch/core.ts:456-459 @ e5425f0`: `return { ...base, outcome: "unreachable", error: \`processing: ${String(e)}\`, failure: "refused-by-source" }`. Point 1 enumerates refused-by-source as "any other `4xx`" plus the browser's missing field; a PDF that no longer decodes or a lost slice marker is neither. Effect: such a day is now green (a lapse) where it was red. Live instance: yes — the watchlist carries `slice` entries.

**On the builder's two judgement calls:** point 4's unstated case (refused-by-us also unread yesterday → outage) is correct — point 3's membership rule is unconditional ("already unread in the previous run's list is an outage") and the colour is red either way; no finding. The `refused` field is finding 3.

Total: 4 findings (1 hard, 3 soft). Worst: **the CLI exit rule and `watch complete` line are unproved** — the slice's whole point, and its only untested line.

### Security

**Scope checked:** data `b4ddd68...e5425f0` (14 files), site `c64ecdf...ceb8c65`. Prior s34 Security lines (IPv6 literals, `unchecked_value_sources`, `cdp.ts:423/425`, the transition-prefix boundary) re-checked: all closed hunks are byte-identical in this range and none of their callers moved.

**Hard 1. An outsider can now keep a source unread indefinitely without ever reddening a run.** `../permit-rulebook-data/src/watch/core.ts:561-570 @ e5425f0`

```ts
for (const source of next.unread ?? []) {
    if (before.has(source.id)) outages.push(source);
    else if (failure.get(source.id) === "refused-by-us") refused.push(source);
    else lapsed.push(source);
}
return { lapsed, outages, refused, red: outages.length > 0 || refused.length > 0 };
```

`before` is yesterday's `unread` list *only*. A source that fails on alternating runs is never on the previous list, so it is `lapsed` and green every time — and on the good day it drops off `unread`, taking `since` with it (`unreadSince`, `core.ts:508-516`: `started.get(r.id) ?? today`). Nothing accumulates anywhere. The class is source-chosen: `failureOfStatus` (`failure.ts:78`) makes any `429`/`5xx` transient and any other `4xx` refused-by-source — both green on day one. Only `refused-by-us` is red at once, and a source cannot elect that. Before this diff `process.exit(unreachable > 0 ? 1 : 0)` (`cli-watch.ts:154-159`, deleted) made every unread source red, which fired `Say so when the run fails`. What an outsider does: a bot wall or an intermittent CDN returns 503 every other morning and the source is read half as often as promised, with no issue, no red, no counter — silencing the threat model's *"A value on a live page goes stale or wrong … **Checked:** the daily watch re-reads every source"* row down to a `warn` line in a log nobody reads. **Live instance:** none for the alternation itself; the flakiness is real — `../permit-rulebook-data/watch/state.json @ e5425f0` carries two unread `bamf` entries today.

**Judgement 2. `causeCode` returns any string, unbounded, into the public run log.** `../permit-rulebook-data/src/watch/failure.ts:88-92 @ e5425f0` — `return typeof code === "string" ? code : undefined;`, appended in `fetch-source.ts:399` as `` `${String(e)} (${code})` ``. Not a fixed set, no length bound, while the repo's standing invariant binds every printed address at 200 chars (`shortAddress`). No attacker-controlled path found (Node/undici own `cause.code`). The message is correctly not taken. **Live instance:** none.

**Judgement 3. A page's own exception text goes into the log unbounded.** `../permit-rulebook-data/src/watch/cdp.ts:382-386 @ e5425f0` — `throw new ReadFailure(answer.exceptionDetails.exception?.description ?? …, "refused-by-source")`. `description` includes the page's stack and is fully source-controlled; it reaches `report.error`, logged raw (`cli-watch.ts:131`, `error: r.error` — `url` is wrapped in `printableAddress`, `error` is not). The string is pre-existing; the hunk moved, so it is in scope. It does **not** reach the flag file or issue (`flagFile` runs only for `changed`/`reminder-due`). **Live instance:** none.

**Judgement 4. An unclassified failure defaults to green and to no retry.** `../permit-rulebook-data/src/watch/core.ts:263-265 @ e5425f0` — `failure?: FailureClass;` optional on `WatchReport` while required on `FetchResult`. A result reaching `verdictOf` with `failure === undefined` falls through to `lapsed` (finding 1's `else`) and is skipped by the retry (`core.ts:487`). The safe default for an unknown failure is red. Type-enforced for in-repo producers, so **live instance:** none.

**Checked and clean.** Retry is the same `judge` → same `fetchSource`/`openBrowserReader`, so the same `refusedAddress`/`refusedTarget`/`MOST_HOPS`/`BUDGET_MS` floor — not a second path. Requests per source are bounded at 2×(1+`MOST_HOPS`), time at 2×30 s; `refused-by-us` is never retried, so a refused address costs zero extra requests. Worst-case run ≈44 min (44 fetched entries) against the job's default 360 min — a source cannot time the job out (the missing `timeout-minutes` is in `.github/workflows/watch.yml`, **outside the delta**). `since` is carried through `printableAddress`-adjacent logging but is itself unvalidated state-file input — no exploit path; repo write required. No per-caller reads, no money, no entitlements in this slice. Scenario file: the site diff appends only inside the licensed correction paragraph (`@@ -155,3 +155,19 @@`, additions only); the held-out text did not move — not a scenario edit.

4 findings (1 hard, 3 judgement). Worst: `verdictOf`'s previous-run-only outage test (`core.ts:561-570`) lets a source stay unread on alternating mornings without ever turning a run red.

### The round's sort (the floor, steward-70 to 76)

Behaviour on running code, no live instance — braked, the human's word asked on STATUS: Security 1 (the alternating-day hole; the rule the human approved in point 3 is the rule that has it).
Security judgement, closing in the delta with its round: Security 2, 3, 4.
Convention, taken in the same delta: Spec soft 2 (a retry case with a prior snapshot); Standards soft 2 (the shape test becomes `not.toContain(origin)`); Standards soft 3 (the glossary loses the status list and the field name).
Graded hard by an axis, ruled a proof-text finding by the session: Spec hard 1 — see below.

## Delta round 1 on 08792f1 (data) · 8902ca0 (site)

The delta (data e5425f0..08792f1, seven commits; site dc2276c..8902ca0): the window rule on the human's word, the three Security judgement lines, the two convention fixes, the glossary and CONTRIBUTING, the correction paragraph 1,125 → 1,108 bytes. Gates at 08792f1 (the builder): 45 files / 782 tests, `npm run check` green, 253.09 s with a real Chrome; site 53 / 757. The runner read the delta once (run 36011906533, `commit=false`, job green in 178 s): 46 sources, `bamf-hochschulabsolvent` `fetch failed (ECONNRESET)` → transient → a lapse with `days: ["2026-09-24"]`, `watch complete … lapsed: 1, outages: 0, refused: 0`, exit 0.

### Security

**Prior open lines.** 1 — closed (`core.ts:594-607, 673-687 @ 08792f1`: fail/ok/fail → `["d1"]` survives the good day, `["d1","d3"]` on the third run → outage → red; migration verified via `knownLapses` `core.ts:621-627`; a future-dated day is kept and ages out; one failure per eight days stays green — the window the human approved). 2 — closed (`failure.ts:114-120`: `A_CODE = /^[A-Z0-9_]{1,40}$/`, tested against a URL-with-token and a 4,000-char code). 3 — closed at the throw (`cdp.ts:384-393`: `shortFailure(…)`, total bound 200). 4 — closed (`core.ts:263-277`: two arms; `met === undefined` → `refused` → red).

**Judgement 1. `lapses` reaches a printed line with no validation and no bound.** `core.ts:596-598` + `../permit-rulebook-data/src/watch/cli-watch.ts:153-154 @ 08792f1`: `id: notice.id, url: printableAddress(notice.url), days: notice.days, today: notice.today,` — `state` is `JSON.parse(...) as WatchState` (`cli-watch.ts:16,31`), nothing checks `lapses`; `url` goes through the sanitiser, `days` through none, an array of unbounded length: a state author can print thousands of arbitrary strings into the public run log — the one printed field left with no bound. A non-array value makes `days.filter` throw and kills the run, against `readJson`'s own promise. Repo write required. Live instance: none.

**Judgement 2. An unparsable day is forgotten, not refused.** `core.ts:577-581`: `Number.isFinite(back)` is false for `Date.parse` NaN, so a corrupted date erases a silent morning green-ward, while the comment documents pruning as old-side only. Repo write required. Live instance: none.

**Outside the delta.** `../permit-rulebook-data/src/watch/cdp.ts:414 @ 08792f1` — `` `HTTP ${served.status} in the browser (after redirect to ${served.url})` `` reaches `ReadFailure`'s text unbounded and unsanitised; `served.url` is chosen by the source and lands raw in the public run log. Not in the delta's hunks. Live instance: none.

Checked clean: no new outbound call, no change to budget, hop or retry bounds; the `since` removal leaks nothing; the site edit stays inside the correction paragraph. 4 findings (0 hard, 2 judgement in-delta, 1 outside; the four prior lines closed).

### Spec

**Hard 1. A `--only` run against the state on disk destroys the migrated silent mornings.** `../permit-rulebook-data/src/watch/core.ts:775-782 @ 08792f1`: `mergeTargetedRun` reads `previous.lapses` only and never runs `knownLapses`, so it does not migrate from `previous.unread` the way `lapsesAfter` (`core.ts:596-604`) does. `../permit-rulebook-data/watch/state.json @ 08792f1` has the two bamf entries unread and no `lapses`: a `--only=<x> --commit` where `x` also fails writes `lapses: {x: [today]}` — truthy — and both bamf mornings are gone; the next full run skips the migration and calls their next silence a green lapse: the hole the word closed, reopened. The added test (`tests/s35.test.ts:445-466`) seeds a state that already has `lapses`, so it cannot see this. Live instance: yes — the state on disk.

**Hard 2. The correction paragraph replaces point 3's colour rule and leaves the rest of point 3 standing and false.** Site `docs/spine/scenarios/s35-…md:166-173 @ 8902ca0` says only the week rule; untouched and now untrue: `:76-77` "A source that answers drops off the list and its `since` with it. `--only` runs merge the list as they do now and keep `since`" — the word says the opposite and `since` was deleted (`state.ts:56-73`); the proof list `:119,123` still names `since: today` and "`mergeTargetedRun` keeps `since`". A reader of body + paragraph cannot tell where the days live or what happens on a good morning. Text, not behaviour. Live instance: the file.

**Soft 3. An unclassified failure is bucketed and logged as `refused`.** `core.ts:683`: red is right, but point 4 and the glossary define refused as "the floor declined to make the request"; a curator reading `watch:refused` hunts an address fault that does not exist. Live instance: none (unreachable by the union type; the test casts).

**Soft 4. The week's edge is untested.** `core.ts:585-591` prunes at `back < 7`; `tests/s35.test.ts:359-371` uses a day 8 back only; a day exactly 7 back is unproved. Live instance: the file.

Re-checked and closed: Spec soft 2 (`s35.test.ts:201-232`, `:234-246`). Spec hard 1 (round 1): `cli-watch.ts:145-155` changed comment and `since:` → `days:` only; still no test, as the session ruled. Proved as asked: alternating red on run 3 (`:325-352`), a day aging out (`:359-371`), a source reading today off `unread` with days kept (`:348-351`, `:373-381`), the full-run migration (`:428-443`), refused-by-us + prior day = outage (`:414-422`). 4 findings (2 hard, 2 soft).

### Standards

Prior findings both closed: Soft 2 (`../permit-rulebook-data/tests/s34-fetcher.test.ts:363-373 @ 08792f1`, `not.toContain(sourceOrigin)` plus the bare host) and Soft 3 (`CONTEXT.md:406-433 @ 8902ca0`, the status lists, the example and `since` gone, `_Avoid_` kept). Hard: none; the scenario edit sits inside the dated paragraph and shrank.

**Soft 1 — two comments each claim to be the only unbounded string.** `failure.ts:61` "…this was the one printed string with no bound at all" and `failure.ts:112` "…this is the one printed string in the watch that had no bound at all" — same slice, two strings; one is false whichever is read second.

**Soft 2 — a number that names no source.** `failure.ts:118` `A_CODE = /^[A-Z0-9_]{1,40}$/`: the alphabet's source is named, `40` is not, unlike `MOST_OF_A_FAILURE`'s measured 10,104 and `THE_WEEK`'s DECISIONS line.

**Soft 3 — a substring assertion.** `tests/s35.test.ts:133` `toContain("characters)")`: the suffix wording is declared in no contract — the same shape as the Soft 2 this delta removed.

Smells (judgement): Primitive Obsession / Data Clump — one source's silence in three shapes (`UnreadEntry`, `UnreadDays`, the bare record), re-joined by hand at `core.ts:679`, `:594`, `:776`; Duplicated Code — the `touched` rule written twice (`core.ts:768` vs `:776-777`). 5 findings (3 soft, 2 smells), 0 hard.

### The delta round's sort

Behaviour on running code with a live instance — fixed, and that delta takes a round: Spec hard 1 (`mergeTargetedRun` must start from `knownLapses(previous, today)`).
Security judgement, closing in delta 2 with its round: the `lapses` boundary (shape, length, a day that is a day — Security judgement 1 and 2, one check); `served.url` through `printableAddress` (the builder's sibling, s35's own hunk — closes here, not handed off).
Rule text, fixed without a round: Spec hard 2 — the correction paragraph replaces, within its bytes, what point 3 and the proof list say of `since` and the good morning.
Convention, taken in the same delta: Spec soft 4 (the day exactly seven back, a test); Standards soft 1 (one comment made true), soft 2 (the source of `40`), soft 3 (the decision asserted, the suffix named by the contract or the length by the bound).
Dismissed: Spec soft 3 and the two smells — below.

## Graded hard by an axis, ruled not a behaviour finding by the session

- Spec hard 1 (`../permit-rulebook-data/src/watch/cli-watch.ts:186-187 @ e5425f0`): the CLI is a script that runs on import and reads its files by `import.meta.url`; a test that executes it needs a path seam, and a delta adds no mechanism (rule 5). The line itself is a one-word map of `verdict.red`, which `tests/s35.test.ts` asserts on both sides. The runner's measurement run (36005247129) executed the line on a lapse: a `warn` `watch:lapse`, `watch complete … lapsed: 1`, exit 0, job green. Exit 1 on an outage is the same line's other branch, not yet met on the runner; the first scheduled run after the merge will likely meet it (the two bamf entries carry no `since`, the correction paragraph says what that means). The proof-list sentence "exit 0 through the CLI's own rule" is shrunk in the correction paragraph by the delta to what is proved: `verdict.red` by test, the CLI's line by the runner.

## Dismissed

- Delta round 1, Spec soft 3 (`../permit-rulebook-data/src/watch/core.ts:683 @ 08792f1`, an unclassified failure logged as `watch:refused`): unreachable by the union type — a producer cannot omit the class — and red is the right colour; `RunVerdict.refused` documents the bucket as "red this morning"; a fourth word for a case the types forbid is speculative.
- Delta round 1, Standards smells (`state.ts:112`, `core.ts:768/776-777 @ 08792f1`): `unread` is what this run could not read and `lapses` is the week's silence per source — two facts, two shapes, joined where the verdict needs both; the `touched` rule twice is two lists, and a third is not asked.
- Spec soft 3 (`core.ts:RunSummary`, `../permit-rulebook-data/src/watch/cli-watch.ts:139 @ e5425f0`, the `refused` field and `failure:` on report lines): accepted — the three buckets are disjoint and sum to `unreachable`, which two fields could not show; the correction paragraph does not carry it because it is not a shrunk promise but an added field, recorded here.
- Spec soft 4 (`../permit-rulebook-data/src/watch/core.ts:456-459 @ e5425f0`, a `processing:` throw classed refused-by-source): accepted as consistent with point 1's own rule for the browser's missing field — a lost slice marker or an undecodable PDF is "the answer was not the page", the source's doing, and takes the source's one day of grace; the human may overrule with the word on the braked open line above, which is the same one-day grace.
- Standards soft 1 (`.github/workflows/watch.yml:153-156`, the step's name and error): the workflow is outside the slice by point 5 and `tests/rebuild-dispatch.test.ts` pins its steps by name; the error prints only on exit 1, when an outage or a refusal *is* an unreachable source; the name is a label read by people and moves with the next workflow change (a roadmap note, not a delta).
- Standards judgement (smells): the empty-body rule twice — the core's is the floor under every fetcher by its own comment (s11), the fetcher's is the fetcher's; `core.ts` gaining the verdict types — the verdict is the engine's decision and belongs beside `runWatch`, the CLI only maps it; the three-class table in five places — one code table (`failure.ts`), one contributor doc, one glossary, one scenario, one comment: each a different reader; `u.since ?? today` — a state written before s35 has no `since`, the guard is the migration.

## What real-green means here

The metric-2 reading of 2026-09-29 over the seven runs ending that day: every red run names an outage or a refusal by us, and no run is red for a source that read clean the next morning (the scenario's proof list). The first scheduled run after the merge is read the morning after for its `watch complete` line.

## Gates at the round's head

Data `npm run check` at `e5425f0` (the builder, 2026-09-24): 45 files / 772 tests, build, typecheck:tests, validate, watch:coverage green, 254.55 s on this machine with a real Chrome. Site: no code change. Runner: run 36005247129 as above.
