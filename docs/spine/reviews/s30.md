slice: s30 — on a phone the next question comes up to meet the reader
branch: s30-question-comes-up (site)
base: 71d168b
head: 4ff0a46
date: 2026-09-18
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other
scope: site only — index.astro (`landOnQuestion`, one call site, `wide` hoisted), scripts/browser.mjs (`emulateMedia`, test-only), tests/s30.test.ts (new), CONTEXT.md (one glossary pair), the scenario's dated correction

Reviewed at `ddb6c30`; the findings landed in `4ff0a46`, verified by the builder's gates (668 tests, astro check 0, assets true) and the session's measurement of the rebuilt local build (390: card 539 → 73 after the first answer, Back likewise; 1280: scrollY 0).

## Standards (7 — 0 hard, 1 should-fix, 6 nit)

1. **should-fix, fixed** — the *reveal* / *landing* pair the code and the correction lean on was in no glossary; recorded in `CONTEXT.md` with the distinction and the avoid-list.
2. nit, kept — a local element named like the imported renderer; renamed `headEl` (fixed).
3. nit, fixed — `|| 0` fallback on a measurement that cannot fail; dropped.
4. nit, kept — `wide` declared once, 1,090 lines above its second reader (one declaration now; verified).
5. nit, fixed — the test's re-typed reduced-motion query explained in a comment (the page declares it as a literal and exports nothing).
6. nit, fixed — `emulateMedia`'s JSDoc promised a feature nothing calls.
7. nit, kept — the sixth copy of the browser harness (precedent).

Verified clean: the scenario diff is the dated correction alone; the gap is the header's computed `padding-bottom` and the height its bounding box — no typed pixel; no user-facing string; no source-grep; assertions are decisions (state, position against the measured landing, focus holder); nothing silenceable by data; LF.

## Spec (5/5 after the fix; 1 partial found and closed)

1. **(a) partial, fixed** — Back after the reader scrolled on an entry left the question above the viewport (measured 390: scrollY 622, card top −177): the browser restores the scroll the reader *left*, not where the landing put it. Now a history move to a question later than the first lands too on a narrow screen, scheduled after the browser's restore (measured: the restore happens after the popstate handler, before the first frame); the first screen keeps the browser's 0; wide screens untouched. The correction paragraph rewritten to say so; the reviewer's walk is now a browser case at both phone sizes.
2. Back to the first question restoring the cold-load view: acceptable (point 5 keeps the first screen).
3. `emulateMedia`: proof tooling, in scope, test-only.
4. Every other hunk attributable to points 1–4; the two "seen, not touched" Back behaviours (a fresh arrival with a restored record; reload-then-Back) are pre-existing on master (blame 2026-09-08/09-15) — filed for a steward pass.

## Security (0 findings beyond the recorded exception)

Verified clean: the shipped hunk reads `matchMedia`, the header's box and computed padding, and calls `window.scrollTo` — no answer or record field reaches a URL, storage key or outbound call; the code lives in the bundled module covered by `script-src 'self'` (no hash change; `csp.ts` untouched); `scripts/` is outside the build; record/escaping/csp suites green. The one flag is the scenario's dated correction, the recorded exception.

**Verdict:** merge-safe at `4ff0a46`; spec met on five points; the partial the Spec axis measured is closed and pinned.
