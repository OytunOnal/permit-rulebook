# Review — s37, Back on a restored record

slice: s37
branch: s37-restored-back
base: 255612e (the scenario's own commit, off master cff34a0)
head: (pending)
date: 2026-09-25
axes: Standards · Spec · Security — three rounds so far, nine reviews

Scenario: `docs/spine/scenarios/s37-back-on-a-restored-record.md` (approved 2026-09-25, "onaylıyorum"; one dated correction paragraph from the build at `8d1b451`).

The build (acc8bfd, e08ab0c, 8d1b451): `src/lib/screen.ts` gains `firstHeld` — the first step of the page's list the browser holds an entry for — with `firstHeldStep(landing, current)` and `backFor(history)`; the interview's "← Back" calls `history.back()` only past `firstHeld` and otherwise steps to the previous question in place; every `pushState`/`replaceState` writes `{ step, firstHeld }`; nothing is pushed on arrival. Gates at 8d1b451 (the builder): 54 files / 772 tests, build 0 errors; four walks red on master (each landing on `/data/`).

**What the build changed from the approved words, said here so the human reads it.** Point 1 and point 4 said the page tells an arrival from a reload by the navigation's type. The build measured that a restored arrival which is then reloaded, or left and returned to, reads `reload` / `back_forward` while the browser still holds only the arrival — the approved design would have sent the screen's Back off the site in exactly those cases (confirmed independently by the Spec axis). The page therefore writes into every history entry it makes how many of its own steps the browser holds behind that entry (`{ step, held }`), reads it back on load, and **no longer reads the navigation's type at all**; an entry it cannot read — including one written by the build before this slice — counts as an outside entry, and the screen's Back steps in place, which never leaves the site. The change is strictly safer and adds no promise to "What a reader gets", but the approval was given to different words.

## Open lines

open with reason: src/lib/screen.ts:321-328 @ 8d1b451 — `firstHeld` is kept as a fixed step number, so when the record grows in another tab the rebuilt list is longer than the one the state was written against: tab A arrives on three answers, tab B (same origin) answers two more, tab A reloads — state `{step:5, firstHeld:3}`, 5 > 3, and the screen's Back calls `history.back()` onto the page the reader came from. Reproduced at both shas by the Security axis (base and head both land on `/data/`). Live instance: yes — the exact fault the slice fixes, through a second tab. Fixed in delta 1 with its round: carry how many steps the browser holds behind the current one, not where they began.
closed: src/lib/screen.ts:321-328 — each entry carries `{ step, held }` and a load derives `firstHeld = max(0, current − held)` (`src/lib/screen.ts:356 @ d65ff41`); proved with two real tabs sharing the origin's storage, red on the round's head and green after; 3581be5
open with reason: src/lib/screen.ts:323-326 @ 8d1b451 — a bad or missing `firstHeld` falls toward "go back in the browser" rather than toward an arrival: a string, `{}`, a negative, `-Infinity`, `0`, or an entry written by the previous build (`{step}` only) after a reload all resolve to 0 and the screen's Back leaves the site; `NaN` sticks as `NaN`. Only a same-origin script can write `history.state`, so there is no outsider path; the real case is a reader with an arrival tab open across the deploy who reloads. Fixed in delta 1: only an integer is believed, anything else is an arrival — the safe side, because stepping in place never leaves the site.
closed: src/lib/screen.ts:323-326 — `readEntry` believes `step` and `held` only as non-negative safe integers (`src/lib/screen.ts:312,331 @ d65ff41`); eleven shapes measured as outside entries; the page reads `history.state` in exactly two places, both through `readEntry`; the navigation's type is no longer read; b0f32b8
open with reason: src/lib/screen.ts:356 @ d65ff41 — `firstHeld` is derived once, on load, and the `popstate` handler (`src/pages/index.astro:1631-1642`) reads only `step` from the popped entry and discards its `held`. So a record that SHRANK in another tab (an outside entry walked to step 8, entry `{8, held 3}`; Start over and one answer in the other tab; a reload: `firstHeld = max(0, 1 − 3) = 0`) lets three screen Backs pop entries that clamp to the same screen and the fourth call `history.back()` onto the page the reader came from — the fault this slice fixes, introduced by this delta (the round's head stayed on the site for the same steps). A record that GREW gives the opposite shape: a dead Back, or the two Backs landing on different questions. Found by the Security axis (hard) and the Spec axis (soft, the same cause). Live instance: none — two tabs on one origin, no outsider. Fixed in delta 2 with its round: `popstate` re-derives `firstHeld` from the popped entry's `held`.
closed: src/lib/screen.ts:356 — `popstate` reads the popped entry through the same `firstHeldStep(state, at)` a load uses (`src/pages/index.astro:1646 @ 01791c7`), and an entry that holds more steps than the list has before it counts none as held (`src/lib/screen.ts:370-374`); in ten two-tab walks in headless Chrome the screen's Back never left the site at head (the round's base left once) and growth by one or two gave no dead browser tap; 20749bd
open with reason: src/lib/screen.ts:370-374 @ 01791c7 — a SHALLOW shrink still gives a dead tap: `return held <= top ? top - held : top` checks the held count against the list, but what makes the entries behind stale is the entry's own `step` standing past the rebuilt list. An outside entrance on three answers, three more answered (entry `{6, held 3}`), Start over and three answers in another tab, a reload: `held` 3 ≤ current 3, `firstHeld` 0, the screen's Back pops `{5}`, which clamps onto the question being asked — a dead tap — and the "← Back" is gone after it. Found by the Spec axis in a model and by the Security axis in real two-tab walks (also open on two + two; B growing to four); the same at the round's base; the screen's Back never leaves the site in any of them. Live instance: none — two tabs on one origin, a record changed under the reloading tab; no outsider. Braked: a behaviour finding on running code with no live instance, asked on STATUS.

## Round on 8d1b451

### Standards

**Hard: none.** `docs/spine/standards.md` exists, names the typecheck and test commands, and carries the dated lint exemption.

**Soft 1 — two new names already mean something else.** `export interface Landing` (`src/lib/screen.ts:288-296`) and `const landing: Landing` (`src/pages/index.astro:1616`) clash with `CONTEXT.md:211-225`'s **landing** (s30: bringing the question card's top under the header) and with `land()` at `index.astro:487-531`; the code and the correction use "arrival" for a fresh open onto a stored record, while `index.astro:51, 357-405` already use *arrival* for an arrival from a country or route page. Of the four terms the build proposes, none is in `CONTEXT.md` yet; "first held step" is new and belongs there. Mysterious Name. Live instance: yes.

**Soft 2 — the entry's state shape is written in one file and read in another.** Written at `index.astro:1633` and `:1744` (`{ step, firstHeld }`), read at `screen.ts:321` and `index.astro:1643`; `tests/history.test.ts:120-137` builds the state by hand, so a rename on the writer side stays green; `firstHeld` lives in both `ScreenHistory` and the state and only line 1633 keeps them in step. Data Clumps, Shotgun Surgery — a state builder exported from `screen.ts` closes it. Live instance: yes.

**Soft 3 — a comment still states the retired rule.** `tests/history.test.ts:20-22`: `"← Back" is history.back() itself`, unconditioned, in a file this range touched. Live instance: yes.

**Soft 4 — numbers in `tests/s37.test.ts` name no source:** `tries >= 4`, `settle(400)` (the harness's own default, unnamed), 700, 500, 1200 at eight sites, and the same answer gesture settling 350 at one site and 450 at another. Live instance: yes.

**Soft 5 — the storage key typed in the test** (`tests/s37.test.ts:51,54`) while `src/lib/record.ts:16` exports `STORAGE_KEY` and the file already imports `RECORD_VERSION` from it. Duplicated Code. Live instance: yes.

**Soft 6 — the correction paragraph** (`docs/spine/scenarios/s37-…md:116-130`, +16/−0) uses the permitted dated form and replaces point 1's clause; its last sentence, "The proof list gains the reloaded and the returned arrival", adds evidence rather than a promise. Live instance: yes.

Checked and clean: the walks compare `.qlabel` to the dataset's own `label` (a decision); the retry helper and the `setTimeout(…, 0)` gestures are explained; no new user-facing string; no country name in the inline script.

### Spec

**Hard 1 — point 6 was not done.** "`CONTEXT.md`'s interview terms say what an *arrival* is against a *reload* for the history, if that distinction becomes a term the code names." The code names it (`firstHeld`, `firstHeldStep`, `backFor`, `Landing` at `screen.ts:290-336`; `index.astro:1616`), and `CONTEXT.md` is not in the diff. Live instance: yes.

**Soft 2 — point 3 has no test after an arrival.** "On every screen reached by answering in this tab, the screen's Back and the browser's Back land on the same question." The arrival-then-advance case taps only the screen's Back; the agreement case covers only a tab walked from the first question. Probed at head: arrival, answer, Back — both gave *qualification*, step 3; arrival, one Back in place, answer, Back — both gave *situation*, step 3. The claim holds; nothing tests it. Live instance: none.

**Soft 3 — the correction changes an approved decision.** Said above, where the human reads it first.

Checks that held: four of the eight walks are red on master (both three-Backs walks, arrival-then-advance, the reloaded and returned arrival), each landing on `/data/`; the other four guard behaviour that should not change. Nothing is pushed on arrival (`history.length` 3 on both shas). Start over behaves the same on base and head. `copy.ts` and the dataset untouched.

### Security

**Hard 1 — the record grown in another tab** (the first open line above), reproduced at both shas; the control — returning with `history.back()` instead of reloading — stays on `/` at head, most likely because the back/forward cache restored the page without rebuilding the list.

**Soft 1 — the fallback leans toward leaving** (the second open line above), measured over nine shapes of `firstHeld`; no run threw.

**Soft 2 — the scenario file was edited by the build.** The axis grades an edit to an acceptance scenario hard by Spine's general rule; this project licenses exactly one dated correction paragraph, and the Standards axis read it as the permitted form. Ruled not a finding — see below.

Checked and clean: the `try` around the navigation-type read fails to an arrival; nothing read from `history.state` reaches the page, a URL or storage; no `localStorage`, `fetch`, `sendBeacon`, `innerHTML` or `location` write added; `threats.md` has no threat touching history.

### The round's sort

Behaviour with a live instance — fixed, and that delta takes a round: the record grown in another tab.
Security judgement in the same function — closed in the same delta: the fallback made safe.
Text and convention, taken in the same delta: Spec hard 1 and Standards soft 1 together (the terms written into `CONTEXT.md` under names that do not collide with **landing** or the country-page *arrival*), Standards soft 2 (one state builder and reader in `screen.ts`, tested together), soft 3, 4, 5, Spec soft 2 (the two Backs proved to agree after an arrival).
Not built: the correction paragraph (Standards soft 6, Spec soft 3, Security soft 2) — it is the licensed form, it replaced a claim and added evidence, and the one thing it changed that the human approved is said at the top of this record.

## Delta round 1 on d65ff41

The delta (f4d21ff..d65ff41, six commits): `{ step, held }` on every entry, one writer and one reader of it in `screen.ts`, only a safe integer believed, the navigation's type no longer read, `openTab()`/`front()` in the shared harness for a real two-tab walk, the terms in `CONTEXT.md` (**outside entry / re-entry**, **held step / first held step**), the tests' waits named, `STORAGE_KEY` imported, point 3 walked after an outside entry. Gates at d65ff41 (the builder): 54 files / 778 tests, build 0 errors, assets ok.

### Security

Both prior lines closed (the two-tab growth, measured six Backs in place at head against the first Back leaving at base; eleven bad shapes all outside entries). **Hard 1 — the record shrunk in another tab, then a reload:** the open line above; the axis modelled the page's handlers in Node rather than driving tabs, and the Spec axis reproduced the same cause in a real browser. **Soft** — the builder's own unfixed item (growth, then a reload, then Back jumps from the sixth screen to the third and the two Backs disagree for a moment), inside this change because the line it lives on (`index.astro:1632`) is this delta's, and closed by the same fix. **Soft** — `readEntry` accepts `held` larger than `step`, which the page never writes (`{step:3, held:2^53−1}` gives `firstHeld` 0 and `backFor` "browser"); only same-origin script can write it. **Soft, harness** — with two tabs, `problems` and `requests` are one list, any tab's `goto` empties it, and the two-tab walk never checks `problems()`. Outside the delta: Start over sets `firstHeld` to 0 while the entries from before it stay behind (the base has the same shape; a reading).

### Spec

**Hard: none.** Every sort item landed; nothing unasked (the harness additions are what the two-tab proof needs; removing the type read is what the fallback finding asked). The two-tab test really uses two tabs sharing storage and goes red against the round's head. **Soft 1 — `popstate` ignores `held`:** 3 answered live, tab B adds one, reload, three screen Backs, then the browser's Back — a dead tap at head; growth of two or more lands the two Backs on different questions. The same cause as the Security hard. **Soft 2 — the correction paragraph:** it only replaced lines, but one line runs to 126 characters and it never says the type is no longer read ("a rule keyed to the type alone" still suggests a combination). Outside the delta: the record's paragraph for the human described the round-0 design — rewritten above; at both shas Back from a grown step skips the grown question.

### Standards

**Hard: none.** Prior 1, 2, 3, 5 closed; 4 partly. **Soft A — "entry" now means three things:** a way of opening the page (*outside entry*), a history entry (the same glossary entry's last sentence, `screen.ts:318`, `entryState`/`readEntry` beside `recordScreen as recordEntry`), and the watch's entries elsewhere in `CONTEXT.md`; and **held step** sits under **Hold line / hold state** with no `_Avoid_` line telling them apart. **Soft B** — two test comments untrue (`tests/history.test.ts:138-140` on what the old code did with `1e308`/`Infinity`/`2.5`; `:263` introducing eight values while `:143` lists nine). **Soft C** — `SETTLE`'s margins (≈32×, ≈15×, ≈5×) carry a reason but no factor, and `offInterview: 400` retypes the harness default and doubles as the retry interval. **Soft D** — the harness's "set up the same way whichever it is" while `problems`/`requests` are shared across tabs (no other caller affected). **Soft E** — the correction paragraph uses "arrival", which `CONTEXT.md` now lists under `_Avoid_`.

### The delta round's sort

Behaviour, the change's own — fixed, and that delta takes a round: `popstate` re-derives `firstHeld` from the popped entry's `held` (the open line above; it closes the Security hard, the builder's unfixed item and Spec soft 1 together), with the shrink and the growth each walked in a real browser with two tabs.
Security judgement in the same function, same delta: `held` believed only when it is no larger than `step`.
Convention, same delta: Standards A (one word for one thing — the history entry keeps "entry", the way of opening gets another; held step told apart from the hold line), B, C, D (each tab its own `problems`, or the shared list said and checked), E and Spec soft 2 (the paragraph says the type is not read, uses the glossary's words, and wraps).

## Delta round 2 on 01791c7

The delta (476c92b..01791c7, two commits): `popstate` reads the popped entry through `firstHeldStep`; an entry holding more steps than the list has before it counts none as held (the builder's departure from the prescribed formula, measured: the formula alone closed growth and not the shrink, because the harmful tap is decided at load and the unanswered question then shows no "← Back"); `held` believed only up to `step`; each tab its own `problems`/`requests`; the waits carry their factors; the way of opening renamed *entrance* (**outside entrance / re-entrance**), "entry" kept for the history entry; the correction paragraph says the navigation's type is not read (1,083 → 1,041 bytes). Gates at 01791c7 (the builder): 54 files / 783 tests, build 0 errors, assets ok.

### Security

The prior line closed (above). **Hard 1 — a shallow shrink still gives a dead tap** (the open line above), walked ten times in real two-tab Chrome at both shas; in all ten the screen's Back never left the site at head (base: once). All three reads of `history.state` go through `readEntry`. The harness change leaves one-tab callers as they were. The rename changes no string a reader sees. The builder's three items graded soft, the same at base, outside the delta, none leaving the site through the interview's Back: Back then Forward onto the question being asked loses its "← Back" (`src/lib/question.ts:131-132`); after a deep shrink the browser's own Back walks stale entries before leaving; Back from a grown step skips the grown question. Outside the delta: `docs/spine/scenarios/s10-cls-harness.mjs:3` imports the primary tree's harness by an absolute path.

### Spec

**Hard 1 — the same finding, found in a model:** the unit assertion `firstHeldStep({ step: 5, held: 3 }, 3) → 0` (`tests/history.test.ts:151`) asserts exactly the dead-tap case, and the browser walk tries only one shrink depth. The builder's departure addresses the round-1 mechanism and the reversed assertion (none held when held exceeds the list) asserts the right decision; the bound is what is wrong. The growth walks do prove the two Backs agree on each screen a Back reaches (they part at base, match at head). **Soft 2 — the correction paragraph** dropped the measured `reload`/`back_forward` readings and the harm line while replacing, and does not mention the shrink rule the glossary carries. Nothing unasked.

### Standards

**Hard: none.** Prior A–E closed (A apart from one sentence). **Soft 1** — `CONTEXT.md:252-253` "the *entry* is the thing `pushState` makes" contradicts the same glossary entry (an outside entrance opens onto an entry the interview did not write) and leaves out `replaceState`'s entries. **Soft 2** — the *held* `_Avoid_` line glosses a held step as an entry, the term says a step. **Soft 3** — `NOT_A_COUNT` inserted between a describe block's doc comment and its `describe`. **Soft 4** — one comment line of 91 characters.

### The delta round's sort — braked

The one behaviour finding has no live instance and lives on running code: the brake. It goes to the human with three options on STATUS, and nothing is built on it until the word:
- **sınır** — the boundary declared in `screen.ts`'s header: the page trusts its browser history within one tab's own walk; when another tab changes the record under an entry, a reload may leave entries whose steps name questions the list no longer has, and the interview's Back may give one dead tap there — it never leaves the site (ten walks measured).
- **düzelt** — the cheap fix with its round: believe `held` only when the entry's own `step` equals the rebuilt list's top, i.e. only when the record did not change under this entry; any change (grown, shrunk, restarted) counts nothing as held and the screen's Back steps in place, which can neither leave the site nor tap dead. Its cost: after a record changed in another tab and a reload, the screen's Back steps in place while the browser's own Back walks its old entries, so the two disagree there (point 3 covers screens reached by answering in *this* tab, and this is not one); the growth walks change from "the two agree" to "the screen's Back is never dead".
- **ayrı** — its own entry; s37 closes as it stands.
The conventions (Spec soft 2, Standards 1–4) ride whichever delta comes next, or are taken without a round at the close.

## Graded hard by an axis, ruled not a finding by the session

- The scenario edited by the build (Security soft 2, graded hard by Spine's general rule): the correction paragraph is the one edit this project licenses a build to make; the held-out text above it is unchanged. Not a finding.

## Dismissed

## What real-green means here

The live site after the deploy: the first walk of the proof list at phone width in headless Chrome against `https://permitrulebook.com/`, and the human's own phone for one tap — a restored arrival, the interview's "← Back", the previous question and not the page before.

## Gates at the round's head

`npm test` at 8d1b451 (the builder): 54 files / 772 tests; `npm run build` 0 errors, 38 pages; `npm run assets:check` passed.
