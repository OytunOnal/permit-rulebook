slice: s26 — one reader, one hierarchy
branch: s26-one-hierarchy (site)
base: 3295690
head: 95f0bd0
date: 2026-09-17
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other (the third because the slice reads a declared answer, `situation_country`, into the rendering; human: "code review 2 eksenli değil 3 eksenli")
scope: site only — situations.ts, copy.ts, index.astro, tests/s26.test.ts (new), tests/s21.test.ts, tests/record.test.ts, tests/wire.ts (new), the s26 scenario's dated correction

Reviewed at `b0d76b4` (the build); the findings below landed in `95f0bd0`, verified by the builder's gates on that commit (633 tests, astro check 0, assets true, fingerprint unmoved) and by the session's walk of the rebuilt preview at 390×844 and 1280×900.

## Standards (7 findings — 0 hard, 1 should-fix, 6 nit)

1. **should-fix, fixed** — the "something open anywhere" unit case asserted `namedFirst(…, undefined)` against a typed constant; the decision (zero open → written state → lead; otherwise none) lived only in `index.astro`. Now `leadCountry(ds, answers, zeroOpen)` in `situations.ts`, called by the page and by the tests, which derive the order from the profile.
2. nit, fixed — `index.astro` re-spelled `namedFirst`'s comparator; one `leads(country, lead)` predicate, exported, used by both.
3. nit, kept — both rest sentences pinned verbatim after the relation is asserted (s19/s21 precedent).
4. nit, kept — the "differ only by the name" proof keys on the article `/^The\b/` (the spec's own proof).
5. nit, kept — the third copy of the browser harness in a test (repo precedent; a shared harness is a slice of its own).
6. nit, kept — `["research:fr"]` is a dataset tripwire in a site test (s21 precedent: it forces a decision when the data moves).
7. nit, fixed — the zero-open walk was named `closed` (CONTEXT.md avoids the word for the not-yet state); `nothingOpen`.

Verified clean: the scenario edit is one dated correction paragraph; no user-facing string outside `copy.ts`; no country name added to an inline script; no source-grep test; comments say why; the new keys are declared answers, no content demanded; no check silenceable by data or fixture; no CRLF.

## Spec (6/6 points met; 4 judgement findings)

1. **(a) fixed** — point 5 ("every other four-country result unchanged") was proved at verdict level only; now measured on seeded screens: the explorer and the offer-in-Germany profile render *Nothing open yet — N steps…* with sections in dataset order and the steps-holding sections open.
2. **(a) fixed** — point 3's second sentence ("any holding an unlocking step opens") was never seen with a named country leading; a profile exists on this dataset (research + France + recognised degree + funds + no German: six German unlocks) and renders `[France open, Germany open, Spain, Netherlands]`.
3. **(c) fixed** — the correction paragraph said two s21 assertions moved; four did (headline and order rewritten; the status pin and the negative dropped there, the first living on in s26, the second inverted). The paragraph is exact now.
4. (b) kept — `lead` on `UnscoredVerdict` was not asked for; declared in the correction, guarded by point 3's condition.

Verified by running: 16 `situation_country × situation` pairs, one marked (`fr × research`); headline and door identical on both paths; `lead` = `fr` on `all` only; the rest sentence differs by the country's name alone; fingerprint fixture untouched, base-path test green.

## Security (1 finding — 0 hard, 1 should-fix)

1. **should-fix, fixed** — the "answers never leave the device" browser check never walked the `all → situation_country` path (it clicks the first option, Germany), and s26's walk opened no request monitor. The check's per-request body is now `tests/wire.ts` (`expectNothingLeft`), used by `record.test.ts` as before and by s26's two walks at both viewports under `network: true`: every request same-origin GET, no body, no query string, no declared answer or route name as a token.

Verified clean: no outbound mechanism added; the only href on the new path is the same-origin route page, `escAttr`-gated, its slug pinned to `[a-z0-9-]` by `address.test.ts` and prefixed by `url()`; every new string reaches the DOM through `esc(subline)`; a forged or unknown `situation_country` is dropped by `restore` and, in memory, yields no verdict and dataset order; no secret, stack or internal id rendered; `csp.ts` untouched, no new inline script or external resource. Threat rows: "answers leave the device" now checked on this path; the rest untouched by the diff.

**Verdict:** merge-safe at `95f0bd0`; spec met; the three should-fixes landed before the word was asked.
