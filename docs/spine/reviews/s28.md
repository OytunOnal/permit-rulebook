slice: s28 — a learn link says what the reader does there
branch: s28-learn-links (site) · s28-learn-links (data)
base: 8438dcb (site) · 8d40bc0 (data)
head: 9a9bea2
head-data: 4b1e983
date: 2026-09-17
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other; each reviewed both repositories' diffs
scope: data — dataset.json (three learn labels), CONTRIBUTING.md (step 9), tests/s28.test.ts; site — card.ts, question.ts, index.astro (glossary parameter dropped where dead), tests/s28.test.ts, seven test files (the parameter), route-page/answers tests (retired premises), data.lock, the scenario's dated correction

Reviewed at data `fa732a0` / site `335a2bb`; the nits landed in the heads above, verified by the builder's gates (data check 618; site 662 green + the one `pipeline` case that waits for the data merge to reach the sibling checkout) and the session's walk of the local build at 390×844 (question help, card box, route page).

## Standards (7 findings — 0 hard, 0 should-fix, 7 nit)

1. nit, fixed — a data test re-asserted the key set the schema already holds (`required` + `additionalProperties: false`); dropped.
2. nit, fixed — a test title claimed a version bump the diff does not make (the version was already today's, s23); the schema pin stays, the version is not asserted, the comment says why.
3. nit, kept — the four labels pinned verbatim (s25 precedent; the scenario's decided sentences).
4. nit, kept — the verb set copied from CONTRIBUTING into the test (the contract declares it; the comment binds them).
5. nit, fixed — two nested stutter patterns in `route-page.test.ts` became one, keyed to the restated number (see Spec 4).
6. nit, fixed — the correction paragraph's numbering and its "three German pages" (two).
7. nit, kept — a route path pinned as a dataset tripwire; the fifth copy of the browser harness (precedent).

Shotgun surgery judged necessary: the dropped `glossary` parameter had no reader left; every touched hunk is the one-token removal; prose glossing unchanged.

Verified clean: the scenario diff is the dated correction alone; the new checks inject a synthetic `§` label so only the renderer satisfies them; no fixture moved (reason stated); no string outside copy.ts or the dataset; no source-grep; comments in voice; no CRLF.

## Spec (4/4 points met; 4 judgements)

Correction (1) a true premise failure: the decided NL sentence broke s5d's standing rule that `IND` is accompanied by "immigration service" (`verdict.test.ts`); the older rule stands, the label reads *…on the orientation-year page of the Dutch immigration service (IND)*. (3) verified in `route-page.ts`: the asks block draws only under an unquoted rule — two German pages carry a door, FR/NL none. (4) verified: neither fixture can move (frozen dataset carries only the Anabin door; `/data/` keys on the unchanged version). Two older tests' premises retired legitimately (`answers.test.ts` "explains the section symbol"; `route-page.test.ts` the blanket `sections? [0-9]`).
1. judgement, fixed — the narrowed stutter pattern missed the citation-line form s23 P1 retired; now `§ N … section N` with a back-reference.
2. judgement, fixed — point 3 asserted href as a filter on the route page; label and href now asserted for every door on every surface, the FR door's card box seeded (no NL hold result exposes its door on this dataset).
3. nit, fixed — "three German pages" → two.
4. nit, kept — the NL sentence could be shorter; the builder mirrored the question's own form.

## Security (1 finding — 0 hard, 1 should-fix, operational)

1. **should-fix, applied at the merge** — `data.lock` names a commit reachable only from the data branch; a site merge before the data merge would fail the deploy's data checkout. Order: data lands first (fast-forward), then the site; the `pipeline` test's "sibling not behind the lock" case turns green with it.

Verified clean: every `learn` render gated by `esc`/`escAttr` on all three surfaces; `learn.url` is `pattern: ^https://` in the schema and validated at the site's build boundary — a `javascript:` URL fails the data check and the build; `rel="noopener"` on all three; no script, no CSP change; escaping/record/csp tests green.

**Verdict:** merge-safe — data at `4b1e983`, then site at `9a9bea2`; spec met on four points; every nit the axes raised landed before the word was asked.
