slice: s25 — the experience ladder is two questions
branch: experience-two-questions (site) · experience-two-questions (data)
base: 0076547 (site) · 53db438 (data)
head: fa05ce4
head-data: 4b2a4ea
date: 2026-09-17
axes: Standards · Spec — two `reviewer` agents, one axis each, read-only, no access to each other; both reviewed both repositories' diffs
scope: data — dataset.json (two fields, ten rules, four readings, two citations), schema 0.8.1, engine.ts, validate.ts, types.ts, tests; site — record.ts, steps.ts, index.astro, tests, fixtures

The first recorded review under steward-58; the pair had been skipped for s14–s24 (DECISIONS 2026-09-17). Run on the uncommitted diffs; the heads above are the commits the fixes below landed in, and the reviewers' findings were re-verified by the builders' gates on those commits (data 612, site 624).

## Standards (10 findings — 2 hard, 2 should-fix, 6 nit)

1. **hard, fixed** — eight re-keyed rules carried `reason: re-read-unchanged` while their notes said no re-read happened (NL's page is on the human tier); `tests/s25.test.ts` pinned the note's prose. The enum gains `rule-rekeyed` and `citation-corrected` (0.8.1); the ten lines carry them; the test counts reasons, never words.
2. **hard, fixed** — the four `seven-year-answer-is-a-floor` readings were named to the reader and absent from `scope.not_asked`; added on all four routes, the `reason` lines revised, the exclusions twin updated, the s19 pin made exact.
3. should-fix, fixed (site) — `AskedField` was a hand copy of the package's `Question` shape; now `Pick<Question, "field" | "options">`.
4. should-fix, fixed (data) — the readings opened with the authority's requirement in the `ours` slot; they open with our reading now.
5. nit, fixed — the same persona mapped differently across data tests; one mapping (`y3in7` → `lt2`,`3to5`; `"no"` → `lt3`); three assertions in `unlocks.test.ts` moved, each recorded as having rested on the false implication.
6. nit, fixed — a test re-implemented `itemFields`; imports it.
7. nit, fixed — doc comments said what, not why; `rows.minItems: 2` documented.
8. nit, **kept as is** — the es-blue-card five-year limb carries two history lines for one day: they are two decisions (a re-key, a citation fix) with two reasons, which is what finding 1 asks the field to carry.
9. nit, fixed — the BOE offset in a note was the offset of "mínimo", not the sentence start (213627).
10. nit, kept — the site's old-rung grep over `src/` asserts absence (the name-sweep precedent).

Verified clean: `itemRows`/`itemFields`/`bestRow` are the only readers of items; best row never summed; a half-answered item stays open; `equivalenceKey` reads through `pointsFor`; `textHistoryEntry` and both `pointsItem` branches keep `additionalProperties: false`; the record's four-key whitelist and the never-leaves-the-device test intact; no CRLF; no old literal in site `src/`.

## Spec (11/11 points met; 3 nits)

Points 1–6 as corrected: met, each with the hunk; point 3 verified by running the engine (`lt2`,`3to5` → hold, 0 pts; `2plus`,`3to5` → met, +2; `2plus`,`5plus` → 3 not 8; no contradiction pairs either field). Proof bullets met; the human's walk was the session's in Chrome. Fixtures: `frozen-dataset.json` migrated; `root-build.json` rightly unmoved (its two routes read neither field); `data-page-clean.json` moved for the schema line only. Nits: a counted claim in `s5e.test.ts` lives in a comment beside a bare hash; the middle rung's label says "3 to under 5 years" where the scenario wrote "3 to under 5" (accepted — the unit belongs on the label); `rows.minItems` and the table's `history` are schema words beyond the correction (documented inline).

**Verdict:** data merge-safe after 1 and 2 (landed in `4b2a4ea`); site merge-safe (`fa05ce4`); spec met on both.
