slice: s32 — a statement names the question that asks it
branch: s32-statement-field (site) · s32-statement-field (data)
base: 0e6475d (site) · 81ceca9 (data)
head: 1a68843 (build 93be021 reviewed; the human's amendment 6 and its delta review below)
head-data: 9bbd24f
date: 2026-09-18
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other; each reviewed both repositories' diffs
scope: data — schema 0.8.2 (`routeStatement.field`), types, scope.ts (`askedByCriterion`), validate.ts (three checks), dataset.json (nine fields, de-researcher's scope, four reasons), CONTRIBUTING.md step 4, tests; site — card.ts (`askedFieldOf`), tests/s32.test.ts, tests/s19.test.ts, CONTEXT.md, the scenario's dated correction

Reviewed at data `520ea6c` / site `e7898cf`; the findings landed in the heads above, verified by the builder's gates (data check 647; site 694, astro check 0, assets true) and the session's read of the two cards on the local build (de-researcher: *scored against your answers* with the hosting agreement in the asked block; de-ict-card: the tenure under *not checked here*).

## Standards (12 — 0 hard, 3 should-fix, 9 nit)

1. **should-fix, fixed** — CONTRIBUTING's step 4 defined a precondition as "no question can reach" and then named the question that reaches it; one true statement now (not *scored*; covered → `field` → "asked"; else "not checked here").
2. **should-fix, fixed** — the site's *Precondition* glossary entry said "never asks"; the s32 exception is in it.
3. **should-fix, fixed** — `statedNotAsked`'s filter comment still explained the retired sentence key; says the field.
4. nit, fixed — `askedByCriterion` kept an unread route parameter and a docstring claiming a site reader; the parameter dropped (nothing external binds it), the docstring true.
5. nit, fixed — two validator checks re-derived in the test beside `validateDataset`; the duplicates dropped, a title made true.
6. nit, fixed — four dataset `reason`s explained an asking by the retired key; reworded to the field (two named by the review, two more with the same words — flagged by the builder, kept).
7. nit, kept — the version pin in seven test files (the practice is the smell; each retirement carries its why).
8. nit, fixed — a site test re-typed a third of `esc`; imported.
9. nit, kept — the seventh copy of the browser harness (precedent).
10–12. nits, kept — the verbatim trigger of `quotedByCriterionUndecided` keys on quote equality with a decision as the remedy (the spec's own point 2); three consecutive loops in the validator (cosmetic).

Verified clean: 0.8.2's reason lives where 0.8.1's does (the schema's own descriptions); the field's pattern is the field id's; the validator keys on declared ids and the route's read fields; de-researcher's `not_asked` edit carries its reason in the contract's form; the scenario diff is the dated correction alone; no source-grep; no fixture moved; LF.

## Spec (5/5; four corrections, all true premise failures)

Verified by running: nine `field`s by id in the dataset, the two tenures untouched and in `not_asked`; the validator rejects an unknown id and an unread field; the cross-check green; the browser walk on de-ict-card. Corrections: (1) no changelog — the reason lives in the schema; the contract sentence a necessity, not a violation of "nothing else"; (2) "asked" route-bound in effect through the validator; (3) `not_asked` is authored (CONTRIBUTING), de-researcher's emptied with a reason in es-researcher's form; (4) the tenures never left *not checked here* — s19's site rule was verbatim-only. Nits, taken: the correction's fingerprint reason pointed at `askedFieldOf` (the route page reads the scope line; the fixture excludes the three routes — the sentence now says so); the "nothing else moves" sweep asserted the implementation against itself (now the eleven by id).

## Security (0 findings beyond the recorded exception)

Verified clean: `field` is a lookup, never printed; the printed answer is the dataset's option label through `esc`; an unanswered or hostile value → null → *not checked here*; the validator rejects an unknown id at the site's build boundary (`assertValidDataset` on every page); no script or CSP change; record/escaping/csp green; the retired pins touch no boundary check.

**Verdict:** merge-safe — data at `9bbd24f`, then the site pinned to the data merge; spec met on five points; every should-fix landed before the word was asked.

## Delta after the review — the human's walk (amendment 6, `93be021` → `7aa7419` → `1a68843`)

Amendment 6 (human: "§ = section yazmaya gerek yok"): the gloss never says *§ = section*; an act's name still expands once in the same bracket; s23's stutter rule stays. Reviewed on Spec and Standards in one report (no script/CSP surface for Security):

- **Spec** met on every line: `SECTION_EXPLAINER` gone; zero occurrences in the built pages; the act expansion once (`§ 18g AufenthG (AufenthG = the Residence Act)`); both stutter regexes kept; the fingerprint regenerated with the reason; seven test files moved with why-comments. H1: *Skilled worker — academic (§ 18b; § = section)* → *(§ 18b)*.
- **Standards** 2 judgements: a spliced comment in `tests/s28.test.ts` (fixed); the retired literal spelled in four files (kept — putting it in copy.ts would revive it).

**Verdict:** merge-safe at site `1a68843` with data `9bbd24f`; 694 tests.
