slice: s34 — the watch reads the IND itself again
branch: s34-browser-strategy (site) · s34-browser-strategy (data)
base: c19f8ae (site) · 47ee7e8 (data)
head: 080a3cb (site — the correction paragraph's two prose replacements, taken without a round; the last commit a reviewer read is 9ff77c1)
head-data: ba003d0 (data — the conventions and the declared boundary, taken without a round; the last commit a reviewer read is 9939496, the "düzelt" delta)
date: 2026-09-24
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other; nine delta rounds, twenty-seven reviews; the reloaded floor (steward-70 to 76) applied from round 8
scope: data — `src/watch/browser.ts`, `chrome.ts`, `cdp.ts`, `steps.ts`, `fetch-source.ts` (new), `core.ts`, `cli-watch.ts`, `.github/workflows/watch.yml`, `watch/watchlist.json`, `watch/state.json`, `tests/s34.test.ts`, `tests/s34-browser.test.ts`, `tests/s34-fetcher.test.ts`, five test files with s14's promises retired, `CONTRIBUTING.md`, `data/verify-s5e.md`, `.gitattributes`; site — the scenario's single dated correction paragraph and four `CONTEXT.md` entries
records: the data repository's files are anchored as `../permit-rulebook-data/<path> @ <data sha>`; the hygiene script cannot resolve a sibling repository's shas, which is reported to Spine as its gap, not hidden by a different spelling

taken without a round: ../permit-rulebook-data/src/watch/cdp.ts:122-125 @ ba003d0 — the comment says what `paused` counts (every paused request, gone or not)
taken without a round: ../permit-rulebook-data/tests/s34-fetcher.test.ts:56-61 @ ba003d0 — six fixture routes no test requested, removed
taken without a round: ../permit-rulebook-data/tests/s34-browser.test.ts @ ba003d0 — one new case pinning the bound at the browser's `anywhere` call site through a real read (red without the wrapper: 9,023 characters)
taken without a round: ../permit-rulebook-data/src/watch/core.ts:746-747 @ ba003d0 — the gate's message says the reason that fired
taken without a round: ../permit-rulebook-data/src/watch/fetch-source.ts:74-79 @ ba003d0 — the "literal address" sentence made true of the two named loopbacks
taken without a round: ../permit-rulebook-data/tests/s34-fetcher.test.ts:175-197 @ ba003d0 — assertions ask the classifier and the constant, not a word and a number; `addressKind`, `AddressKind`, `MOST_OF_AN_ADDRESS` exported for it
taken without a round: ../permit-rulebook-data/src/watch/fetch-source.ts:81-93 @ ba003d0 — the floor's boundary declared in its header (the human's word "sınır", 2026-09-24)
taken without a round: docs/spine/scenarios/s34-the-watch-reads-the-ind-itself-again.md @ 080a3cb — two prose replacements in the correction paragraph: the `link` sentence now says what the code does, the final-code claim names the three runner runs; net bytes down

open with reason: ../permit-rulebook-data/src/watch/fetch-source.ts:84-95 @ 9414594 — the floor under a `link` entry's `anywhere` redirect policy checked literal host strings and missed IPv6: `[::ffff:7f00:1]` (127.0.0.1) was followed from a public entry and a loopback listener's body came back as the reading; `[::ffff:a9fe:a9fe]` was attempted. No live instance. Braked, the human's word "düzelt".
closed: ../permit-rulebook-data/src/watch/fetch-source.ts:84-95 — IPv6 literals parsed (`::` expanded, an embedded dotted quad, the mapped form taking the IPv4 rule), nine shapes refused end to end with zero requests; the "düzelt" delta (data 9939496), gate row 2026-09-24
open with reason: ../permit-rulebook-data/src/watch/core.ts:738-740 @ 9414594 — `unchecked_value_sources` keyed on `kind`, so `"kind": "sentinel"` beside `"strategy": "link"` on a dataset-cited entry greened the gate. No live instance. Braked, the human's word "düzelt".
closed: ../permit-rulebook-data/src/watch/core.ts:738-740 — the gate keys on the dataset's citation; the two-word flip refused and named; the "düzelt" delta (data 9939496), gate row 2026-09-24
open with reason: ../permit-rulebook-data/src/watch/cdp.ts:423 @ 9414594 — the browser reader's `anywhere` branch returned the raw `location.href` with no address bound; unreachable today (`anywhere` is `link`'s alone, pinned by a test). No live instance. Braked, the human's word "düzelt".
closed: ../permit-rulebook-data/src/watch/cdp.ts:423 — `shortAddress` on that branch, pinned through a real read; the "düzelt" delta (data 9939496), gate row 2026-09-24
open with reason: ../permit-rulebook-data/src/watch/fetch-source.ts:134-149 @ 9939496 — the parsed floor still classes the transition and legacy prefixes public: NAT64 `64:ff9b::/96`, 6to4 `2002::/16`, the compat form `::/96`, site-local `fec0::/10`; each embeds or means another address and each was requested from a public entry in the reviewer's probe. No live instance; no source in the watchlist redirects this way; a link's bytes are never hashed, so the harm is the request. Braked, the human's word "sınır".
closed: ../permit-rulebook-data/src/watch/fetch-source.ts:134-149 — the floor declares its boundary in its header (data ba003d0): the literal families it reads, and the transition prefixes and resolved names it leaves to the resolve-time address check under `later`; a corner inside that line is a lie by an address, not a finding; gate row 2026-09-24
open with reason: ../permit-rulebook-data/src/watch/cdp.ts:425 @ 9939496 — the browser's `anywhere` branch has no address floor and no origin check, and `refusedTarget` has no browser-tier counterpart; unreachable while `STRATEGIES` gives `anywhere` to `link` alone, which a test pins. No live instance. Named by the Security axis in rounds 8 and 9.
closed: ../permit-rulebook-data/src/watch/cdp.ts:425 — inside the same declared boundary: a browser row given `anywhere` is a change to `STRATEGIES` that the `["link"]` test turns red, and the resolve-time check is the mechanism for both readers; gate row 2026-09-24

## What the slice is

A `browser` watch strategy: headless Chrome on the runner opens a source, performs the entry's declared steps, waits to settle, hands back the document; from there the html strategy's read. Seven entries left the human tier — the five IND route pages (behind a *Your situation* form), the BMI notice (a cookie round-trip), EUR-Lex (a 202-and-empty-body challenge); Legifrance and gesetze stay. `human_tier` 40 → 1, `verified` 156 → 195, the dataset untouched.

## The loop's shape, stated plainly

Nine rounds. Rounds 1–7 ran under the old floor; the reloaded skills (steward-70 to 76, the human's word 2026-09-24) applied from round 8. **Under the new floor, most of rounds 2–7 would have been brake asks, not builds**: nearly every hard finding on running code carried `live instance: none` — the reviewers' fixtures, not a repository input. The one exception was `learn-anabin` (round 7): a live `link` entry the runner refused under round 3's guard, which read `ok` on master. The rounds already run stand as done; their fixes are real and measured. Round 8's three corners were braked, as the floor asks; the human chose "düzelt" for them and "sınır" for round 9's one, and the record closes with none open.

## Security (nine rounds — 9 hard closed; the five open lines above closed by the düzelt delta and the sınır boundary)

Each closed finding measured on the real reader against fixture origins, with a working control.

1. (r1) A step's click navigated the tab and the reader handed back the landed page — a third party's 200 or 404 as the entry's reading. Closed by an origin check after the steps and the status gate re-run; `expand` skips links.
2. (r2) The origin baseline was where the tab landed, so a source that 302s or meta-refreshes to a third party on load was read there. Baseline = `new URL(entry.url).origin`, checked twice; the BMI cookie chain still reads.
3. (r3) The Document handling was not frame-scoped: a same-origin iframe cut a 10.3 s read to 3.7 s with the shell. Main-frame id from `Page.getFrameTree`; status and in-flight clear scoped to it.
4. (r4) The fetch tier followed redirects unchecked and requested the off-site target before judging it. `redirect: "manual"`, judged first, zero requests to a refused address, the redirect's own status reported, a hop cap.
5. (r5) `Network.setExtraHTTPHeaders` caused a CORS preflight naming `x-source-contact` on every cross-origin request and broke the page's simple GET. The header is added at continue time, same-origin only; the far origin sees one `GET`, no `OPTIONS`.
6. (r5–6) The credentials family: the gate printed the raw URL; `report.url` carried the password into flag, log, `asked` and issue; the browser never refused a credentialled entry (Basic auth sent, to the favicon too). Refused before anything opens, one owner (the fetcher's rule); `report.url` stripped where reports are made.
7. (r6) The wait was not frame-scoped: a genuinely cross-site iframe made a page `unreachable` at 45 s. Only main-frame requests count toward settle.
8. (r7) `anywhere` had no floor: `data:` and loopback followed, link-local attempted. http(s) only, no userinfo, never link-local, loopback/private only for an entry of that class — and round 8 found the IPv6 literal past it (open line 1).
9. (r7) The identifying headers reached every host a page embeds — `Fetch` interception scopes the contact header to the entry's origin; the UA name is stripped off-origin.

Standing evidence at `head-data:` — nine body shapes refused with the control carrying; the `pages.yml` verbatim pin red on 18 mutations (a `github.ref` guard executed for real on the branch: state committed, the site dispatch skipped); off-origin refusal with `net.Socket.prototype.connect` and `dns.lookup` hooked: zero sockets, zero lookups to any unnamed host across twelve reference shapes; the interception measured at 31–103 ms per entry (74–86 paused requests on an IND page); the runner reading all seven with the final code (run 35939456782: IND 20–23 s stepped, EUR-Lex 4.6 s, BMI 9.5 s).

## Spec (eight rounds — 4 hard closed; 9 of 9 points met at `head:`, with the braked corners open)

1. (r2) `expand`'s furniture narrowing was declared in three places and implemented in one arm. Both arms filter.
2. (r3) Point 3's "the day it was measured from the runner" left six of seven notes. Restored per entry.
3. (r6) `urls_with_credentials` was undeclared. Declared.
4. (r8) The correction paragraph grew by a new clause about `link` that the reloaded rule forbids and that `refusedTarget` contradicts. To be shrunk by the build, without a round (prose).

The held-out text throughout: the 11,364 bytes before the correction hash `48634db1…` at every sha; one paragraph. Declared deviations in the paragraph: the form's two faces and the build's own "form is gone" error; the option is "Turkish", the suggestions are links, the radios are clicked by their labels; `expand` opens only content, never furniture; the five IND baselines rendered on the runner; the three gaps — a service worker's own context, the main frame alone, the session-wide UA override; point 5's `Fetch` interception with its two costs; the fetch tier's redirect refusal and the per-strategy policy; the proof list's local run substituted by runner dispatches; the `--only --commit` re-read of one html entry before the merge. Round 8's declarations (this record, not the paragraph): the floor under `anywhere` and what it gives up; `unchecked_value_sources`; malformed addresses refused where credentialled ones are; one owner for the redirect rule on both readers, pinned to `["link"]`; `report.url` an exact URL minus userinfo; the interception's cost measured and not the cause of EUR-Lex's 26.4 s (run 35936470807), which run 35939456782 read at 4.6 s — the runner's slow minute, the retry slice's to carry.

## Standards (eight rounds — 1 hard closed)

1. (r5) `new URL(url).origin` outside the fetcher's `try`: one malformed entry URL killed the whole pass. Inside; the rest reads.

Judgement findings fixed along the way: `browser.ts` split at birth into `chrome.ts` / `cdp.ts` / `steps.ts` / `browser.ts` with a header map; six prose-equality checks and every source-text grep replaced by decisions — including three `watch.test.ts` cases that read `cli-watch.ts` as text and broke when the fetcher moved; the five IND notes from ~2,300 to ~900 characters with the shared story in `verify-s5e.md` §3; the 30 s budget, the 200-character bound and the redirect rule each given one owner; `settle`'s docstring made true; `.gitattributes` `*.ts text diff` for a NUL-carrying file that diffed as binary. Left as follow-ups: `WATCH_NAME`/`WATCH_CONTACT` spelled in two files; `cdp.ts` at 556 lines carrying an outbound-identity concern worth its own file.

## Round 9 — the "düzelt" delta, and the close

Security: nothing hard; the three open lines closed by measurement (nine IPv6 shapes refused from a public entry with zero requests; the two-word flip refused and named; the bound proven on the export). Two judgement corners, both `live instance: none`: the transition prefixes (open line above, closed by "sınır") and the browser's `anywhere` floor (open line above, the same boundary). Spec: 9/9, nothing hard; the paragraph's remaining `link` sentence and the "final code still keeps" clause replaced without a round; the byte count the build reported was off (measured 9,675 → 9,520). Standards: six judgement, all taken without a round (the lines above); the six `/v6-…` fixture routes removed. The change closes on this delta: its own hunks carry no behaviour finding with a live instance, and the two corners it left are declared, not silent.

## Round 8, under the reloaded floor

Security: 2 hard (open lines 1–2), 3 judgement — DNS rebinding (the floor checks the literal host; the code's own comment declares this gap, so it is a lie inside a declared boundary, not a finding), the browser's `anywhere` branch (open line 3), the scenario "edited" (ruled below). Spec: 2 hard — the paragraph's +33 bytes (prose, taken without a round by the build), and `lastCost` logging the previous page's `paused`/`pausedMs` under a failed entry's id (running code, no live instance: no run log is kept in the repository) — braked, asked on STATUS; 3 judgement — the disproved 26.4 s in the paragraph (prose), point 6's line covering the job not the entry, the missing bound (open line 3). Standards: 5 judgement — `refusedAddress` called "credentials" by its caller; two orphaned comments in `fetchSource`; `paused`/`pausedMs` counting differently; an unused import; one `toMatch(/not an address/)`.

## Graded hard by an axis, ruled not a finding by the session

docs/spine/scenarios/s34-the-watch-reads-the-ind-itself-again.md @ 265bfd9 — the Security axis graded the correction paragraph a held-out-scenario edit (rounds 1, 8). The paragraph is the one channel `docs/spine/standards.md` licenses for the build to correct its own record; the 11,364 bytes before it were re-hashed every round and never moved; round 8's lines 207–236 are inside the paragraph, not in *What this slice is not*. Kept out of `Dismissed`: a Security-axis grading is never dismissed.

## Dismissed

../permit-rulebook-data/src/watch/browser.ts:87-94 @ 9414594 — the caller names `refusedAddress`'s result `credentials` while it refuses three things; a name and a comment, fixed without a round after the word.
../permit-rulebook-data/src/watch/fetch-source.ts:186-203 @ 9414594 — two comments orphaned by the move of the credentials check; prose, fixed without a round after the word.
../permit-rulebook-data/src/watch/cdp.ts:113-127 @ 9414594 — `paused` counts every paused request, `pausedMs` only those whose continue resolved; the reason is beside them; the name is the finding; fixed without a round after the word.
../permit-rulebook-data/src/watch/browser.ts:4 @ 9414594 — an unused `RedirectPolicy` import; fixed without a round after the word.
../permit-rulebook-data/tests/s34-fetcher.test.ts:173 @ 9414594 — `toMatch(/not an address/)` beside two lines that already assert the decision; fixed without a round after the word.
../permit-rulebook-data/src/watch/cdp.ts @ 9414594 — 556 lines with an outbound-identity concern (~66 lines) that could be its own file; not split at the end of an eight-round loop, recorded as a follow-up.

## Two known gaps, declared

- The reading is the main frame's document alone: a source that moves its requirement list into a subframe while its lede and footer stay put would match its markers and record a shell. Declared in the paragraph and the Browser tier entry; not closed.
- A source that registers a service worker fetches the worker's own script and its later requests in a context this reader does not identify itself in; the reading does not change (measured), only the identification.

## What real-green means here

The proof list asks for the first scheduled run after the merge reading the seven with no `unreachable` among them, state committed, the site's pin landing with `human_tier: 1`, and `/data/` saying *re-read daily* of the IND's pages. The runner has read the seven on the final code three times on the branch (runs 35926499955, 35936470807, 35939456782); the scheduled run on master is the one that counts.

## Gates at `head-data:`

`npm run check` — coverage ok (`invalid_steps: []`, `urls_with_credentials: []`, `unchecked_value_sources: []`), quotes ok (`verified: 195`, `missing: []`, `human_tier: 1`), 741 tests / 44 files; no state write after the runner's baselines. Site: s11, gloss, name-sweep 24 passed, 1 skipped.
