slice: s29 — three data corrections
branch: s29-data-corrections (data) · s29-pin (site, the lock only)
base: 76c1d52 (data)
head-data: 60f5a82
head: 279082e (site: the pin and two date pins turned into derivations; not reviewed — a lock line and two test derivations)
date: 2026-09-17
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other
scope: data only — dataset.json (info_url, two sources on the free-movement notice, the Algerian door), watchlist.json (two entries), state.json (the IND baseline), verify-s5e.md §6, src/lang.ts (the language gate's prefix), CONTRIBUTING.md (step 9 binds notices), tests/s29.test.ts, eight retired date pins; the scenario's dated correction

Reviewed at `0b84dc9`; the findings landed in `60f5a82`, verified by `npm run check` (636). The session read the three screens on a local site build linked to the data worktree: the Opportunity Card's *Official page* → bmi.bund.de; the Swiss passport's *No work permit needed* screen with three quotes; the Algerian door on the French route pages.

## Standards (7 — 1 hard, 1 should-fix, 5 nit)

1. **hard, fixed** — a test satisfied by the word "notice" appearing in CONTRIBUTING; deleted (the decision lives in s28's widened door test).
2. **should-fix, fixed** — two regexes on a watch note's prose (`cookie`, `400`); deleted; the tier decision stays asserted.
3. nit, fixed — `#13` grep on the history note; the old-URL check is the decision.
4. nit, fixed — the version pin duplicated s23's; dropped.
5. nit, kept — seven `newest_retrieved_at` pins retired with why-comments; the pinning practice is the smell, the derivation is proved once in s8.
6. nit, fixed — the cookie measurement written three times; the test comment now points at the note.
7. nit, kept — the fifth BOM-strip helper (precedent).

Verified clean: the scenario edit is the dated correction alone; `state.json`'s change is exactly the watch's own `--only --commit` write (hash = sha256(text), slice fingerprint, `last_run`/`unread` untouched); both quotes verbatim (fidelity 154 → 156); `legal_basis` in the notice's form; `NON_AUTHORITIES` named as #13's decision; `lang.ts` narrower, not wider; LF.

## Spec (4.5/5 — point 1 partial on two true premise failures, both corrected in writing)

Correction 1 reproduced by the reviewer: the BMI page answers a cookie-less fetch 307 → cookie check → **400**; with the cookie 200 and the sentinel present. So the entry is human-tier (90 days), which the spec's own fallback allows — but "watchable" in the human's choice of A now means a person's quarterly tick until a cookie-returning fetcher exists (said to the human before the merge word). Correction 2 verified: a route has no history slot (`additionalProperties: false`), the watch entry's own `history` is the dated record. Correction 3 legitimate: `lang.ts` (the gate) and `verify-s5e.md` (CONTRIBUTING's human-tier rule) were forced. Correction 4: the version's same-day convention by practice. The hard finding is Standards' (content checks); fixed. Unasked, kept: the `handbookgermany.de` prefix dropped from the gate (defended in the comment).

## Security (0 hard, 0 should-fix, 1 nit)

Verified clean: all six new URLs `https://` under the schema's patterns; the IND slice is the page's own prose after the normaliser (scripts, styles, tooltips, tags stripped) and nothing acts on the stored text — hashed, diffed, quoted into a flag a person reads; the human tier fetches nothing in the daily job (`fetches: false`); the `lang.ts` prefix is a full-path `startsWith`; workflows untouched, actions pinned. Nit, pre-existing: `watch/state.json` is a package export, so sliced source prose ships to package consumers (the site reads only `last_run` and the unread list).

**Verdict:** merge-safe at data `60f5a82`; the site follows by a pin commit; spec met with the two premise failures recorded and one of them (the tier) put to the human.
