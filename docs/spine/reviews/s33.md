slice: s33 — a cached page still finds its assets
branch: s33-carry-assets (site only; no data change)
base: d80ce9b
head: 1cc7800 (trunk, after the merge — it carries `3e0d5bd`, the no-ff merge of the reviewed branch whose every commit is below, and `c720ed7`, the deploy bot's own one-line `data.lock` write, which no reviewer read and which this record does not cover)
head-reviewed: 3e7ba76 (last commit a reviewer read: c8b45d8; the prose fix above it was taken without a round — the lines below name every hunk in it)
date: 2026-09-23
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other; nine delta rounds, eighteen reviews
scope: `scripts/carry-assets.mjs` (new), `.github/workflows/pages.yml` (one step and its comment), `tests/s33.test.ts` (59 cases), `tests/pipeline.test.ts` (four s33 cases), `CONTEXT.md` (four glossary entries), the scenario's single dated correction paragraph

taken without a round: docs/spine/scenarios/s33-a-cached-page-finds-its-assets.md:173-181 @ 3e7ba76 — the boundary sentence gains the mismatched-quote clause (a quoted string whose quotes do not match is read too)
taken without a round: docs/spine/scenarios/s33-a-cached-page-finds-its-assets.md:184-202 @ 3e7ba76 — the request-accounting sentence rewritten: a name the live host no longer serves is filtered before `asked` is incremented, so it costs zero requests and two log lines
taken without a round: scripts/carry-assets.mjs:233-243 @ 3e7ba76 — `short`'s reason line made true of its six call sites
taken without a round: scripts/carry-assets.mjs:356-364 @ 3e7ba76 — `readAssetNames`'s docstring gains the same mismatched-quote clause
taken without a round: scripts/carry-assets.mjs:370-387 @ 3e7ba76 — the docstring's copy of the cost clause, corrected with the scenario's
taken without a round: tests/s33.test.ts:1245-1260 @ 3e7ba76 — the boundary case's comment, corrected with the other two

open with reason: .github/workflows/pages.yml:67 @ 3e7ba76 — the step fetches from `$SITE_URL`, a repository *variable*, not a secret and not a reviewed value. Whoever can set it can point the step at a host they control, serve a page naming a real previous-generation asset and a digest list attesting hostile bytes under that name, and have those bytes published from our own origin, where `script-src 'self'` covers them and no SRI exists to check them (`src/lib/csp.ts`; no `integrity=` anywhere in `src` or `public`). Measured: the mechanism does exactly this shape. The digest list moves the authority out of the HTTP framing, never off the origin, and cannot — the origin is the trust boundary. Rounds 4 to 9 narrowed what that variable can do (it can no longer become a flag, disarm the list, or reach a host the page names) but not this. No code closes it and no other entry may take it; only the human's word does, by pinning the origin to a reviewed value or by accepting the risk on the record.

## What the slice is

`/` is served with `Cache-Control: max-age=600` — GitHub Pages' header, not ours — while `/_astro/*` is content-hashed and replaced on every deploy, so a reader who returns inside those ten minutes asks for a stylesheet and a module that answer 404 (verified live 2026-09-23: `index.CpGG1ZDr.css`, `index.BcmUcb7-.css`, `index.qYLZ_mcb.js`). The deploy now carries the previous generation forward — one generation, read from the live site, never failing the deploy.

## The loop's shape, stated plainly

Nine delta rounds. The mechanism held under measurement from round 3 onward; **every hard finding after round 3 was in the same place — whether the step says anything when it does not work.** Round 4 added partial carrying and left the alarm's predicate in the old world; round 5 fixed the alarm and the set it read was wrong; round 6 fixed the set and the bucket boundary was wrong; rounds 7 to 9 were the log's and the record's own truth. No round ever found a wrong byte published. This record keeps the two apart: the mechanism's evidence, and the alarm's history.

## Security (seven rounds — 11 hard, all closed; the entry above stays open)

Each closed finding was measured on the real process against raw-socket origins, and each guard has a working control so it is not a blanket refusal passing refusal tests.

1. The first cut fetched resolved hrefs, not names — an off-origin fetch. Names only, the URL rebuilt from our origin, `redirect: "error"`.
2. The carrier could exit 1 (a mid-body drop, an unwritable dist, a 300-character name) and so could kill the deploy. Full containment, `uncaughtException` / `unhandledRejection`, exit 0 on every path — 15 of 15 process probes.
3. Every `content-length` guard was the origin vouching for itself: no header, a header that lies low, `content-length: 0`, and `identity` declared over gzip bytes each put a file of the wrong length on disk under the previous generation's exact name and reported it carried. Closed by moving the authority to `asset-digests.txt`, a list our own build publishes; nine shapes refused, the control carries.
4. The 20-asset ceiling counted successes, not attempts: 400 references that all 404 spent 401 requests. It counts attempts now; a mixed 75-reference page spends exactly 21.
5. `$SITE_URL` was spliced into argv, so `SITE_URL=--dry-run` or `--dist=…` shipped an artifact with no list — the mechanism permanently off, the deploy green, from a value no review reads. The address travels as an environment value; 16 flag-shaped values refuse and still publish the list.
6. The page parse was quadratic and uninterruptible: 1 MB took 76.1 s under a 5 s budget, so the 4 MB ceiling was about twenty minutes of CPU inside the deploy job. Linear now (2 MB in 246 ms) under a page ceiling of its own.
7. A reference reduced to its basename scored `_astro/../real.css` as "already in this build" and ended the run silent; a subpath origin carried a file for a URL the deploy never serves. A reference counts only when it resolves to the URL the step would build for its name.
8. Every refusal that returned before the page was read was as quiet as a healthy deploy — seven paths. `looked` separates "could not look" from the rest.
9. `carried.length === 0` was the wrong alarm once partial carrying was normal: one carried, one left behind, no warning, and the reader still 404s. Any needed name not carried raises it.
10. The credential guard held only for passwords made of the characters its class allowed, and then only for values containing `://` — while the population reaching it was exactly the values that had failed to parse. The regex is gone: a value that did not parse is never printed. 27 shapes, zero leaks.
11. A same-origin `/_astro/` reference left in the `refused` bucket predicted a reader's 404 and raised nothing — four shapes. The bucket boundary is now where a reference resolves.

Standing evidence at `head:` — nine body shapes refused with the control carrying; the pinned block in `pages.yml` red on 18 mutations; twelve off-origin reference shapes with `net.Socket.prototype.connect` and `dns.lookup` hooked showing **zero sockets and zero DNS lookups** to any unnamed host, including the digest-list fetch and redirects on the page, the asset and the list; the ceiling counting bytes (a 4 MiB multi-byte body carried whole, 4 MiB + 1 refused); 221 fuzz strings on which the page reader and the list reader disagree on none.

## Spec (seven rounds — 2 hard, both closed; 5 of 5 points met at `head:`)

1. Point 4 asks the workflow-shape test for three properties and it asserted two — "cannot fail the job" was dropped when round 1's source-grep test was removed, and nothing replaced it. The step's whole block is pinned verbatim now, with its position before `upload-pages-artifact`.
2. The refusal split was half-applied: `unlisted.length === needed.length` made an ordinary deploy carrying one stray reference refuse the whole run and post a `::warning::` claiming the window was open. The verdict is asked of the page's whole reference set.

The scenario's held-out halves are byte-identical to approval throughout: head half `8a13f370646f0857…`, proof list `1625c3376fb9e984…`, `Corrected` count 1, one paragraph. Three deviations are declared inside that paragraph: point 3's authority (a carried byte is checked against our own build's digests, which needs the previous origin to have published a list), point 5's one extra published file, and point 1's narrowing. **The narrowing went to the human with the merge word and was ratified the same day ("tamam", 2026-09-23; DECISIONS gate row).**

## Standards (seven rounds — 1 hard, closed)

1. The hand-written YAML reader in `tests/pipeline.test.ts` had a dead block-scalar branch — five of the build job's fifteen steps parsed as `run: "|"` — and read only keys at exactly eight spaces, so a nested `env:` was invisible and a step pointed at a foreign host passed every assertion. The parser is deleted; the step's block is pinned verbatim.

Judgement findings fixed along the way: six prose-equality checks replaced by the decisions they were about; two source-text greps removed; `--budget-ms` and `DEFAULT_ORIGIN` removed as public surface the scenario never asked for; comments made true of the code at a dozen sites; the name rule spelled once; `CONTEXT.md` given four glossary entries.

## Dismissed

docs/spine/scenarios/s33-a-cached-page-finds-its-assets.md @ 95396fd, 17f3ed0, 0035c3f — the Security axis graded the correction paragraph a held-out-scenario edit in four rounds. Dismissed each time: `docs/spine/standards.md` licenses exactly one channel for the build to correct its own record, a dated correction paragraph, and every edit was inside it; the head half and the proof list were re-hashed each round and never moved. The fourth grading raised a different and correct point — that the paragraph narrows an approved requirement rather than correcting a misstatement — which was not dismissed but carried to the human, who ratified it ("tamam", 2026-09-23).

scripts/carry-assets.mjs:910-923 @ c8b45d8 — the dry run promises `would publish` and `would carry` on a `dist` that is a file or absent, where a real run prints ENOTDIR or ENOENT. No deploy reaches it: `astro build` makes `dist/`, and the deploy passes no argv, which `tests/pipeline.test.ts` pins. Terminal-only log truthfulness, and taking it is a code change this closing commit is not.

scripts/carry-assets.mjs:630 @ c8b45d8 — `from` is a dead field beside `where`; the naming finding was answered by adding a field rather than renaming, leaving two fields for one idea and one of them unread. The cost is a reader's confusion, not behaviour.

scripts/carry-assets.mjs:259 @ c8b45d8 — `noBuildDirectory` judges `dist.trim()` while its callers use the raw value, so `--dist " dist"` passes the guard and writes into a directory named with a leading space. The same shape the file records as a defect for the origin. Terminal-only; the deploy passes no `--dist`.

scripts/carry-assets.mjs:646-664 @ c8b45d8 — the block headed "never print a value that did not parse" states an absolute and then excepts the larger population. Both rules are right and both are written down; the headline reads wider than it governs.

scripts/carry-assets.mjs @ 3e7ba76 — the file is 1,024 lines at 674 comment / 313 code and crossed two comment lines per code line this round. Not dismissed as wrong: the seam is named below and deferred deliberately, because a refactor touching every line of a security-critical file is at its worst at the end of a nine-round loop.

tests/s33.test.ts:1460 @ c8b45d8 — the wall-clock margin's binding constraint on a healthy run is its 100 ms floor, not the ratio the comment explains. The case still fails on a quadratic shape; the explanation is incomplete, not false.

## Deferred, named, and queued

`scripts/asset-digests.mjs` — the digest list is already its own thing: its own `CONTEXT.md` term, its own vocabulary, its own import in two test files, and it needs the carrier only for the bounded-fetch pair. About 185 lines and a third of the comment mass: the header's trust section, `writeAssetDigests`, `readAssetDigests`, `getAssetDigests`, `DIGESTS_NAME`, `DIGESTS_HEADER`, `DIGESTS_LINE`, `MAX_LIST_BYTES`, `digestOf`. It gets more expensive every round it waits.

## Two known gaps, stated rather than hidden

- A carried body reassembled across more than one chunk is proved only through the exported `bodyWithin` and a hand-made three-chunk generator. Every route through a socket reintroduces the wait that made a control case fail three times in five full runs on a starved shape — which would have failed s33's own deploy, since `pages.yml:199` runs `npm test` in the build job.
- The half-body case carries two marks because neither covers all three starvation shapes: the positive mark on the step's own wording goes red on the two that never reach a body, and the absence of undici's timeout sentence is red on a clock firing mid-body. Both are in the case, with the measurement.

## Gates at `head:`

`npm test` 757 passed / 53 files (twice at c8b45d8, once at 3e7ba76) · `astro check` 0 errors, 0 warnings, 38 pages · `assets:check` true · `check:base` 38 pages and 32 endpoints under a subpath, no root-absolute internal URL · live dry run against `https://permitrulebook.com/`: `0 asset(s) would be carried, 2 already in this build, 0 left behind`, exit 0, ends quiet.

## What real-green means here, and when it can be read

The proof list says today's two live assets (`index.BEnxC2y6.css`, `index.astro_astro_type_script_index_0_lang.Bev-BElT.js`) must still answer 200 after this slice's deploy. That bullet **passes on this deploy without a byte being carried**: the slice touches nothing under `src/`, so both assets rebuild under the same hashes, `needed` is empty, and the carrier returns after one page request. The real check is later and in two parts, because `https://permitrulebook.com/asset-digests.txt` answers 404 today — the first deploy publishes the list and carries nothing.

On the first deploy after this one that changes a file under `src/`: `/asset-digests.txt` answers 200 and begins with the list's header line, and the log carries `published asset-digests.txt: N asset(s)`. On the deploy after **that**: the previous generation's two `/_astro/` names still answer 200, and the log names them as carried. That is the real-green, and this record carries it because the proof list cannot.
