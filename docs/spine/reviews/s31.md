slice: s31 — the hold state says where it is, and two small things
branch: s31-hold-state (site)
base: 782bf98
head: 05c62af
date: 2026-09-18
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other
scope: site only — index.astro (the hold line in the masthead, the verdict cover as display:none, landOnVerdict's focus, the gutter), question.ts (`holdLineHtml`), scripts/browser.mjs (`scrollbars`, test-only), tests/s31.test.ts (new), tests/first-paint.test.ts (the verdict reader's box asserted 0), CONTEXT.md, the scenario's dated correction

Reviewed at `860af27`; the findings landed in `05c62af`, verified by the builder's gates (687 tests, astro check 0, assets true) and the session's capture of the hold state with the module held 4 s (header, eyebrow, the line — nothing else).

## Standards (6 — 0 hard, 2 should-fix, 4 nit)

1. **should-fix, fixed** — *hold line / hold state* was in no glossary; recorded in CONTEXT.md beside reveal/landing, which gains the focus clause.
2. **should-fix, fixed** — the s10 "the box a returning reader meets" comment contradicted the verdict reader's new case; narrowed to the readers owed a question, pointing at the s22/s31 block.
3. nit, fixed — `landOnVerdict`'s JSDoc says the focus is taken on every gesture-drawn verdict.
4. nit, fixed — the hold line's dress shared with `.sub` in one rule.
5. nit, fixed — sentence-shape assertions beside the decision dropped.
6. nit, fixed — a bare `//` in the harness.

Verified clean: the scenario diff is the dated correction alone; the hold line reuses `RETURNING_LINE` (no new literal); no source-grep; the held time is the scenario's own 4 s; the gutter and edge cases read measurements; LF.

## Spec (4/4; two true premise failures, both corrected in writing)

Point 1 re-measured by the reviewer with the gate's method: finished-record CLS **0 / 0 / 0** at 390×844, 390×1400, 1280×900, no shift source; the held DOM is brand, line (8 px under it), nothing else with a box; the headline lands at the line's own top. Point 2: the s20 return is `wasResult` **false** (✎ sets the questions state before drawing) — focus is taken on every gesture-drawn verdict, none on a first arrival or a history move (Back is outside the slice, filed). Point 3: at 1280×900 the question page is already taller than the viewport, so the spec's viewport could not show the defect; measured at 1280×1400 with real scrollbars: master 464 → 456.5, s31 456.5 → 456.5; the cold page's 7.5 px is the gutter on both states — the trade point 3 names. Point 4: the harness option is proof tooling; the first-paint test's assertions only strengthened (the verdict reader's box 0).

## Security (0 findings beyond the recorded exception)

Verified clean: the pre-paint script untouched (CSP hash unchanged, suite green); the hold line is a build-time constant through `esc`; `scripts/` outside the build; `.stand-in` removal walks the build's own DOM before any write; record/escaping/csp green.

**Verdict:** merge-safe at `05c62af`; spec met on four points; both should-fixes landed before the word was asked.
