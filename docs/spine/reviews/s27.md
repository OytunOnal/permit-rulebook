slice: s27 — five small slips
branch: s27-five-slips (site)
base: dc828b1
head: 66c0820
date: 2026-09-17
axes: Standards · Spec · Security — three `reviewer` agents, one axis each, read-only, no access to each other
scope: site only — copy.ts, identity.ts, not-found.ts, question.ts, route-page.ts, index.astro, identity.css, tokens.css, tests/s27.test.ts (new), identity/route-page tests, two fingerprint fixtures, the s27 scenario's dated correction

Reviewed at `5a6fff1` (the build); the findings below landed in `66c0820`, verified by the builder's gates on that commit (648 tests, astro check 0, assets true, both fingerprints regenerated with the reason) and by the session's walk of the rebuilt local build at 390×844 and 1280×900.

## Standards (9 findings — 0 hard, 4 should-fix, 5 nit)

1. **should-fix, fixed** — the H1 test demanded the name end in a letter or bracket (a content shape); it now asserts the H1 equals the name the page's data gives it and carries no full stop, in the unit and the dist case.
2. **should-fix, fixed** — the 404 single-carrier check was case-sensitive on a lowercase spelling (re-adding the hidden H2 passed); keyed now on the sentence from `NOT_FOUND_START`, case-folded, exactly one carrier, and that carrier the element `aria-labelledby` names; the duplicate assertion dropped.
3. **should-fix, fixed** — `.tagline` re-typed the hero's weight, line and family; it takes `font: var(--text-hero)` with its own size (`--text-hero-size × .82`). Both fingerprints moved again (the shared sheet), regenerated with the reason.
4. **should-fix, fixed** — the 404's privacy sentence was still inline in the re-homed hunk; `NOT_FOUND_START_NOTE` in `copy.ts`. The file's other inline copy is outside the slice.
5. nit, kept — content pins (`MENU`, the nbsp, the H1 literal, the four names): dataset tripwires, s19/s21/s26 precedent.
6. nit, kept — the browser harness copied a fourth time (precedent; a shared harness is its own slice).
7. nit, fixed — `em` renamed `tagline` in the identity test's heading reader.
8. nit, fixed — the route-page comment sits on the markup it describes.
9. nit, fixed — doubled full stop in a fixture's `source` sentence.

Verified clean: menu words reach the script as `data-word-*` attributes and `MENU_SCRIPT` carries neither; no country name in an inline script; no source-grep; `--text-hero-size` a real split; fixtures with reasons; comments in voice; no CRLF; no `visually-hidden` heading remains.

## Spec (6/6 points met; 3 nits)

Points 1–6 met with the hunk each; point 4 measured over 276 ledger rows (24 fields, 17 money bands included) at the sidebar's real width; point 5's `<title>`/`og:title` asserted unchanged on every route; the `/data/` fixture's move attributed to point 1 (the header on every page). The correction paragraph's two claims are true premise failures (the mark needed `white-space: nowrap` on the name — measured −12 px otherwise; two fingerprints, not one). Nits, all taken: the pen proof tightened to the spec's "top equals" within 2 px (passes as-is); the 390 tally place asserted as one of two named positions rather than behind an `if`; the doubled stop.

## Security (1 finding — the recorded exception)

1. **flagged, no change** — the scenario file was edited in the slice commit: the dated `**Corrected 2026-09-17, by the build:**` paragraph, the form s19–s26 used and the reviewer brief names; not a change to any proof case.

Verified clean: every interpolation in the changed hunks gated by `esc`/`escAttr` or a build-time constant; `MENU_SCRIPT` writes `textContent` from attributes the same template renders; the CSP hash is computed from the imported constant at build and `tests/csp.test.ts` walks `dist` (no stale hash possible); no href, query, fetch or beacon added; record/escaping/csp tests green; no secret, stack or internal id.

**Verdict:** merge-safe at `66c0820`; spec met; four should-fixes and six nits landed before the word was asked.
